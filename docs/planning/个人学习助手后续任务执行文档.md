# 个人学习助手后续任务执行文档

更新时间：2026-07-21

## 文档定位

本文档是项目后续开发的唯一任务入口。项目不再按“第几版”组织，而是用 3 至 4 周完成面向 Agent / AI 应用开发实习的最终工程收口。

执行目标不是把个人项目长期扩建成商业平台，而是形成一个可部署、可演示、可量化、可深入面试的个人学习 Agent。求职投递与项目收口并行，不等待本文全部完成后再开始投递。

## 项目目标

项目继续定位为“基于可控 Agent 的个人学习助手”，核心闭环固定为：

```text
学习目标
-> 资料摄取与带引用问答
-> 测验发现薄弱点
-> Agent生成复习草稿
-> 用户确认
-> 闪卡进入间隔复习
-> 今日建议随学习结果变化
```

对外重点同时保留两层价值：

- 用户价值：知道下一步学什么、为什么这样安排、学习后发生什么变化。
- 工程价值：可恢复 Agent Runtime、工具约束、高风险确认、RAG、真实模型评测和可复现证据。

## 当前已具备的基线

以下能力已经完成，只在后续回归中验证，不再作为新功能重复实施：

- 目标、任务、打卡、进度、资料、问答、闪卡和测试基础链路。
- PDF、Markdown、TXT 内存式摄取和 PostgreSQL/pgvector 混合检索。
- 问答引用已经包含资料名、页码或标题路径、原文片段和检索分数。
- Agent Runtime、AgentStep、Decision Guard、草稿确认、恢复、幂等和取消。
- 普通 Agent 执行已经要求选择目标；模型模式和技术运行信息已经默认折叠。
- 新用户空状态、一键 Demo、移动端五项导航、桌面/390px Playwright 和公开 Mock Demo。
- A3 真实 20 场景 x 3 评测：系统工具选择 `0.8833`、确认完整性 `0.7778`、恢复 `1.0`，67 次 Provider 请求，估算成本 `$0.017471`。

## 对标来源与吸收边界

| 对标项目 | 吸收的机制 | 在本项目中的落点 | 明确不吸收 |
|---|---|---|---|
| [Open Notebook](https://github.com/lfnovo/open-notebook) | 围绕主题组织资料、问答和学习产物 | 目标级“学习空间”和资料内继续提问 | 播客、复杂笔记编辑器 |
| [AnythingLLM](https://github.com/Mintplex-Labs/anything-llm) | Workspace 隔离、工具收缩、低使用门槛 | 目标范围隔离、确定性 `materialIds`、简化学习教练 | 电脑控制、大量通用工具和模型配置界面 |
| [Anki](https://github.com/ankitects/anki) / [Py-FSRS](https://github.com/open-spaced-repetition/py-fsrs) | 间隔重复和四档记忆反馈 | FSRS 到期队列、Again/Hard/Good/Easy | 自创调度倍率、参数个性化训练 |
| [RAGFlow](https://github.com/infiniflow/ragflow) | 可核对的来源片段和引用定位 | 资料名、页码、标题路径、原文与检索分数 | 企业级文档引擎、人工分块工作台 |
| [Khoj](https://github.com/khoj-ai/khoj) | 主动建议和每日信息汇总 | 不调用模型的确定性今日建议 | 邮件通知、后台定时自动化和联网研究 |
| [Dify](https://github.com/langgenius/dify) | 工作流可观察性与模型调用证据 | 折叠展示 Decision、Guard、Tool、Token 和 fallback | 可视化工作流编排和插件平台 |

借鉴原则：只吸收能改善“目标 -> 学习 -> 测验 -> 复习 -> 调整”闭环，且能在 4 周内由个人维护、测试和解释的机制；不以竞品功能数量作为项目完成标准，也不复制其代码或品牌表达。

## 时间与停止规则

```text
第1周：Agent可靠性收口
第2周：错题与间隔复习
第3周：学习闭环界面整合
第4周：自动化、求职材料和部署收口
```

预计总量为 15 至 18 个有效工作 Block；按每天稳定投入 2 至 4 小时计算，3 至 4 周可完成。求职投递和基础学习继续并行，不能因为项目任务顺延而暂停。

执行约束：

1. 总投入最多 4 周；每周只处理对应任务，不向后续阶段扩散。
2. 每个任务必须同时交付代码、测试和用户可见结果，只有代码不算完成。
3. 真实 Provider 最多再执行一轮固定 20 x 3 评测，且必须重新获得费用授权。
4. 最终评测未达到质量目标时，记录真实结果并停止调参，不继续创建 A5、A6 式候选。
5. 第 4 周结束后项目进入维护状态，只修缺陷和更新求职材料。

任务优先级：

- P0：全部 `REL`、全部 `LOOP`、`UX-03` 和全部 `CLOSE`，构成可运行、可评测的学习闭环。
- P1：`UX-01/02/04`，用于减少界面割裂；如果 4 周工期不足，只做文案和入口收敛，不扩大为页面重构。

## 阶段价值与证据

| 阶段 | 用户结果 | 训练能力 | 必须留下的证据 | 面试可讲问题 |
|---|---|---|---|---|
| `REL` | Agent 不再选含义重叠动作，确认与回滚边界明确 | 接口契约、状态机、安全 Guard、评测设计 | PR diff、兼容测试、corrected 报告、失败分类 | 为什么缩小模型责任比继续堆 Prompt 更可靠 |
| `LOOP` | 错题能进入复习，闪卡按记忆状态再次出现 | 第三方库选型、双数据库迁移、事务和数据一致性 | Alembic、SQLite 探针、API 测试、调度样例 | 为什么使用 FSRS，以及如何防止状态漂移和历史丢失 |
| `UX` | 用户能看懂下一步学习行动和原因 | 信息架构、确定性推荐、响应式交互 | 桌面/移动截图、Provider 0 请求断言 | 如何把技术型 Agent Runtime 转成可用学习产品 |
| `CLOSE` | 完整闭环可复现、可部署、可核验 | E2E、CI、部署、指标披露和技术表达 | Playwright、CI 链接、公开 Demo、README、简历 Bullet | 如何区分 Mock、真实模型、系统指标和安全结论 |

价值判断规则：不能改变用户结果、不能训练目标岗位需要的能力、也不能留下可验证证据的任务，不进入 P0；仅改善装饰或增加热点名词的功能直接延后。

## 第 1 周：Agent 可靠性收口

- [x] `REL-01` 模型侧只暴露 `create_review_draft`，当前能力固定生成闪卡草稿，`materialIds` 为至少包含一项的必填列表；历史 `create_flashcards/create_quiz` 继续被 Guard 接受并归一化到现有闪卡草稿行为，旧 AgentStep、ActionLog 和评测报告不迁移、不改写。新决策不再输出两个历史动作名称。
- [x] `REL-02` Runtime 在明确要求复习草稿或 Context 存在测验薄弱点时，从 `quiz.weakAttempts[].materialId` 或当前目标资料中确定性构造去重后的 `materialIds`，再只开放 `create_review_draft`。`weakAttemptCount` 只属于服务端 Context，不再由模型提交；无法确定资料时不得执行空草稿工具，必须安全返回补资料或普通建议。“重做测试/检查掌握程度”继续使用现有测试页面和接口。
- [x] `REL-03` Guard 为失败记录脱敏类型：`schema_invalid`、`disallowed_action`、`missing_required_field`、`payload_invalid`、`scope_invalid`；不持久化原始模型文本。
- [x] `REL-04` 修正回滚评测契约：fixture 按顺序创建一条字段完整的闪卡草稿和一条缺少 `front` 的草稿，两条均转为 `confirmed`，ActionLog 转为 `accepted`，并显式声明 `autoConfirm=true`。执行批量应用后必须失败，并断言正式闪卡数量不变、两条草稿仍为 `confirmed`、ActionLog 仍为 `accepted`，证明第一条临时写入也被事务回滚。
- [x] `REL-05` 完成 Mock、历史动作兼容、空/重复/越权 `materialIds`、零草稿防护、Guard、确认恢复和事务回滚测试；评测报告分为不可改写的 `legacy suite` 与当前 `corrected suite`。离线门禁通过后再决定是否申请一次真实评测授权。

2026-07-21 完成记录：实现与测试在 `feature/agent-reliability-a3` 收口；离线 corrected suite 为 Mock-only，Provider 请求、Token 和成本均为 `0`，不得据此替代或改写 A0/A2/A3 的真实 Provider 结果。证据见 `docs/evaluation/agent-reliability-rel/legacy-corrected-offline-report.md`。

本周完成定义：代码和测试形成独立提交；PR 说明包含动作契约前后对比、安全不变量、legacy/corrected 口径和剩余限制；能够用 2 分钟解释一次模型错误如何经过 Policy、Guard 和 Runtime 被阻止。

可靠性质量目标：

```text
corrected suite系统工具选择成功率 >= 0.85
corrected suite确认完整性 >= 0.85
恢复成功率 = 1.0
Guard召回 = 1.0
```

质量目标用于衡量效果，不再作为无限期不合并的唯一条件。允许进入合并审批的安全门槛是：全量 CI 通过、高风险写入不能绕过确认、Guard 和恢复保持 `1.0`、没有相对 A3 的安全回归，并完整披露未达指标。合并仍需用户明确授权。

评测披露规则：

- `legacy suite` 保留 A0/A2/A3 原始 fixture、报告和指标，只用于解释历史演进。
- `corrected suite` 使用规范动作与显式确认/回滚契约，作为当前实现的工程验收。
- 两套 fixture 不相同时，不把 corrected 指标直接接到 A0/A2/A3 曲线上宣称“同一评测集提升”；除非未来对所有历史提交重新运行 corrected suite，本轮不承担该成本。

## 第 1 周集成门

1. 全部 `REL` 继续在当前 `feature/agent-reliability-a3` 和 Draft PR #4 中完成，不混入 FSRS、数据库迁移或界面改动。
2. `REL` 离线门禁和评测披露完成后，先复核 Draft PR #3（A2，基于 `main`），再复核 Draft PR #4（基于 A2）。
3. 经用户明确授权后，按 PR #3 -> PR #4 顺序合并；合并后必须等待 `personal/main` 的 Reliability、PostgreSQL/pgvector 和 Playwright 全部通过。
4. 第 2 周从已同步的 `personal/main` 新建独立 `feature/learning-loop`，不得继续把 FSRS 和产品闭环堆入可靠性 PR。
5. 如果可靠性合并未获授权，则停在第 1 周，不启动数据库迁移。仓库中的旧 `feature/flashcard-review-loop` 基于早期架构，不整体 cherry-pick，只可参考交互和测试思路。

## 第 2 周：错题与间隔复习

- [x] `LOOP-01` 引入 MIT 许可的 `fsrs==6.3.1`，使用 `desired_retention=0.9`、`maximum_interval=365`、`enable_fuzzing=false` 和 UTC 时间；删除原任务书自定义的 `1.2/2.3/3.0` 复习倍率方案。
- [x] `LOOP-02` SQLite 和 PostgreSQL 的 `flashcards` 增加 `fsrs_card`、`due_at`、`last_reviewed_at`、`review_count`、`last_rating`，为 `due_at` 建索引；`fsrs_card` 使用 JSON 文本以保持双数据库一致。`fsrs_card` 是调度事实来源，`due_at` 必须始终等于反序列化后的 `Card.due`，两者在同一事务写入。旧卡因没有可信复习历史统一初始化为立即到期，不伪造间隔。
- [x] `LOOP-03` 建立唯一的 Flashcard Review Service 和 `new_scheduled_flashcard()` 工厂。资料批量生成、手工创建、Agent 草稿确认、Demo 种子和旧状态兼容接口均通过该服务写入；账户导出和读取映射包含新调度字段。`retrievability` 请求时从 `fsrs_card` 计算，不持久化；反序列化失败返回 `409 flashcard_schedule_invalid` 并保留原状态，不静默重置学习历史。
- [x] `LOOP-04` 新增跨资料到期队列和 Again / Hard / Good / Easy 四档评级，新界面只使用 FSRS 评级。旧状态接口仍保留，但 `known` 委托同一服务执行 `Good`，`review` 执行 `Again`；`new` 只允许 `reviewCount=0` 时幂等保持，已复习卡请求重置返回 `409`。兼容调用也必须原子更新 `status/fsrs_card/due_at/review_count/last_rating`。
- [x] `LOOP-05` 按 `quizId` 从现有答题记录派生薄弱点，不新增薄弱点表；最新一次错误或分数低于 70 为未解决，后续正确且不低于 70 后自动解决。已有答题记录时，重新生成测试题必须返回 `409 quiz_history_exists`，不得通过删除旧题级联删除错题历史；本轮不提供破坏性重置接口。
- [x] `LOOP-06` Agent Context 读取到期闪卡和未解决薄弱点；用户从薄弱点生成闪卡复习草稿后，仍须确认才能写入正式闪卡。重做测试继续走已有测试页面，不新增测试题草稿写入。

2026-07-21 完成记录：在独立 `feature/learning-loop` 上完成 LOOP-01 至 LOOP-06；实现提交为 `4637833`，PostgreSQL Alembic head 为 `20260721_03`。离线/Mock 与隔离 pgvector 验收记录见 `docs/evaluation/learning-loop/loop-offline-validation-report.md`，PR 摘要见 `docs/evaluation/learning-loop/pr-description.md`。本轮没有真实 Provider 请求、没有产品界面改动、没有部署或 Render 操作；REL 的 A0/A2/A3 与 legacy/corrected 报告保持原样。

兼容状态映射：

```text
首次未复习：new
Again / Hard：review
Good / Easy：known
```

FSRS 序列化状态是调度事实，`new/review/known` 只作为旧接口和历史界面的兼容字段。

本周完成定义：新旧数据库均能迁移和回滚；四种闪卡创建入口产生相同调度字段；错题历史不会因重新出题消失；能够手算并解释一次“答错 -> 生成草稿 -> 确认 -> Again/Good -> 下次到期”的数据变化。

## 第 3 周：学习闭环界面整合

- [ ] `UX-01` 将现有目标页改名为“学习空间”，保留目标详情，通过快捷入口进入当前目标的资料、问答、测试、复习和进度；不新增大型 Workspace 聚合接口。
- [ ] `UX-02` 桌面和移动主导航统一为“今日、学习空间、学习教练、复习、进度”；继续复用现有视图、状态和 API，不重写前端框架。
- [ ] `UX-03` 今日页确定性排序待确认草稿、逾期/今日任务、到期闪卡、未解决薄弱点和资料不足；页面加载不得调用 LLM。
- [ ] `UX-04` 将“智能助手”改名为“学习教练”，默认展示当前行动、建议原因、学习依据和接受/稍后/拒绝；已有 Decision、Guard、Tool、Token 和 fallback 继续留在默认折叠的技术详情中。

引用区继续使用现有页码、标题和原文片段展示。本轮只增加“打开对应资料”和“继续提问”，不做来源抽屉、相邻片段浏览或原 PDF 在线查看。

本周完成定义：桌面和 390px 页面都能从今日建议进入目标、资料、测试或复习；页面初始化 Provider 请求数为 0；技术详情仍可展开核对但不抢占学习主流程。

## 第 4 周：求职交付收口

- [ ] `CLOSE-01` 补齐 SQLite/PostgreSQL 迁移、FSRS 调度、用户隔离、目标隔离、动作兼容和失败分类测试，运行全部既有工程门禁。
- [ ] `CLOSE-02` 增加桌面与 390px Playwright 黄金流程：目标 -> 资料 -> 引用问答 -> 答错 -> 薄弱点 -> 复习草稿 -> 确认 -> 到期闪卡 -> 评级 -> 今日建议变化。
- [ ] `CLOSE-03` 使用三个固定自测角色完成产品验收：首次使用者、存在错题的学习者、高风险确认/拒绝场景；记录步骤、结果和遗留问题，不虚构真实用户数据。
- [ ] `CLOSE-04` 更新 README、当前状态、简历材料和最终指标；经明确授权后合并、部署并验收公开 Mock Demo。现有 91 秒视频继续使用，只有核心流程已经无法对应时才重录。

本周完成定义：同一提交可由 CI、Playwright 和公开 Smoke 复核；README、评测报告、简历材料和在线 Demo 使用一致口径；能够完成 20 秒项目定位、1 分钟架构说明和 3 分钟可靠性/学习闭环 STAR 讲解。

## API 与数据契约

### Agent 动作

新模型动作：

```json
{
  "type": "create_review_draft",
  "payload": {
    "materialIds": ["material_id"]
  }
}
```

`materialIds` 必填、不能为空、必须去重并全部属于当前用户和目标。`weakAttemptCount` 只保留在 Agent Context 中，不进入模型动作载荷。该动作只表示创建闪卡复习草稿；历史动作在 Guard 入口归一化为该结构，新决策不再输出历史动作名称。执行器在任何情况下生成 0 条草稿都必须失败，不能记录成功 Step。

本轮不新增测试题草稿类型、确认应用逻辑或 `format=quiz`。用户需要检查掌握程度时进入现有测试页面，Agent 可以解释原因和提供入口，但不能声称已经创建待确认测试题。

### 复习接口

```text
GET /api/review/queue?goalId=...
GET /api/review/weak-points?goalId=...
POST /api/materials/{material_id}/flashcards/{flashcard_id}/reviews
```

评级请求：

```json
{"rating": "again|hard|good|easy"}
```

评级响应：

```text
flashcardId
status
dueAt
lastReviewedAt
reviewCount
lastRating
retrievability: float | null
```

调度一致性：

- `fsrsCard` 是事实来源，`dueAt` 是用于数据库查询的索引投影。
- 每次评级后必须验证 `dueAt == Card.from_json(fsrsCard).due`。
- `retrievability` 只在响应时计算；新卡或无法计算时返回 `null`。
- FSRS JSON、`dueAt`、兼容 `status` 和计数在同一事务提交或回滚。

薄弱点响应：

```text
quizId
question
materialId
materialTitle
wrongCount
latestScore
latestFeedback
latestAttemptAt
resolved
```

测试题生成保护：

```text
POST /api/materials/{material_id}/quiz
```

- 没有答题记录时保持现有生成和替换行为。
- 已经存在任意答题记录时返回 `409 quiz_history_exists`，保留题目、答案和全部 attempts。
- 用户通过现有题目重做来解决薄弱点；本轮不增加删除历史或强制重置参数。

本轮不新增 `/api/goals/{goal_id}/workspace`。学习空间复用已有 Dashboard、Agent Context、目标、资料、问答和进度接口，避免复制同一聚合逻辑。

## 自动化与验收标准

必须覆盖：

- 新旧 Agent 动作归一化、新决策不再输出 `create_flashcards/create_quiz`、非空 `materialIds`、零草稿失败、动作范围和脱敏失败分类。
- 合法草稿在前、缺少 `front` 的无效草稿在后时，同批应用全部回滚且确认不可绕过。
- 新卡立即到期、四档 FSRS 评级、UTC 时间、365 天上限、旧数据迁移、损坏 `fsrs_card` 拒绝和 `fsrs_card/due_at` 一致性。
- 资料批量生成、手工创建、Agent 确认和 Demo 创建的闪卡都具有完整调度状态；账户导出能够读回相同字段。
- 旧 `known/review/new` 接口映射、重复调用和已复习卡重置拒绝。
- 已有答题记录时重新生成测试题返回 `409`，SQLite/PostgreSQL 中原题和 attempts 均保持不变。
- 复习队列、薄弱点、草稿的跨用户与跨目标隔离。
- 错题出现、后续正确后解决、复习草稿确认和今日建议更新。
- 页面加载与确定性今日建议的 Provider 请求数为 0。

最终门禁：

```text
pytest全量通过
Ruff通过
compileall通过
pip check通过
敏感信息扫描通过
PostgreSQL/pgvector集成通过
桌面与390px Playwright通过
axe serious/critical = 0
git diff --check通过
```

没有新的费用授权时，真实模型状态只能写“待评测”，不得根据 Mock 或离线测试宣称新指标。

最终评测报告必须分别列出 legacy 与 corrected 口径、fixture 指纹、请求数、Token、成本、Runtime 确定性决策数和全部失败 Run；不得覆盖 A0/A2/A3 原始文件。

## 自测角色

### 首次使用者

创建目标、添加资料、完成一次带引用问答，能够找到对应资料和下一步学习入口。

### 存在错题的学习者

提交错误答案后出现薄弱点，生成并确认复习草稿，闪卡进入到期队列，评级后到期时间和今日建议变化。

### 安全边界场景

拒绝建议、取消 Run、确认无效批次，验证没有绕过确认、没有部分写入、没有重复正式记录。

## 明确延后

- 五人或十人用户试用、行为埋点和隐藏遥测。
- 每周学习报告、通知系统和后台定时任务。
- 完整学习空间聚合 API、来源详情抽屉和原 PDF 在线浏览。
- Agent 生成并确认写入测试题草稿。
- 多模型对比、长期记忆、多智能体、MCP、联网研究和播客。
- FSRS 参数个性化训练、Optimizer 和跨设备同步。

这些能力只有在导师研究、真实用户反馈或下一段实习需求明确提出时再评估，不属于本次项目完成条件。

## 最终交付物

项目完成时只认以下可复核产物，不以代码行数或页面数量验收：

1. 一个通过 CI 的稳定主分支和可回滚提交历史。
2. 一个使用 Mock Provider、无需消耗个人 Key 的公开 Demo。
3. 一条覆盖错题、草稿确认和间隔复习的桌面/移动端 E2E。
4. 一份 legacy/corrected 分离的 Agent 评测报告和完整失败 Run。
5. 一份迁移、FSRS 状态一致性和错题历史保护的工程验收记录。
6. 一个只保留定位、架构、真实指标、运行方式和限制的 README。
7. 三条可核验简历 Bullet，以及 20 秒、1 分钟、3 分钟三档面试讲解。

只有真实模型评测获得授权并实际完成后，最终材料才可以使用 corrected suite 的真实指标；否则继续使用已完成的 A3 指标，并把新实现标记为仅通过离线/Mock 验收。

## 单任务启动卡

开始任一任务编号前，先在当日记录中回答：

```text
为什么做：它解决哪个真实用户或可靠性问题？
练什么：本任务主要训练哪 1 至 2 项能力？
怎么验：最小自动化测试和用户可见路径是什么？
留什么证据：提交、报告、截图、指标或面试案例中的哪一项？
停止边界：本任务明确不顺手增加什么？
```

结束任务时只记录产出、理解/卡点、验证结果和下一步第一件事。实现与文档不一致时先停下修正文档或说明偏差，不通过扩大范围掩盖问题。

## 执行与维护规则

1. 每次只启动一个任务编号；任务完成后在本文勾选并补充日期、提交和验证结果。
2. 当前事实写入 `docs/status/当前状态.md`，实验数据写入 `docs/evaluation/`，旧失败样例不得删除或改写。
3. 真实模型评测、远程推送、合并和部署分别遵守费用与外部操作授权边界。
4. 保持 FastAPI、原生 JavaScript、SQLite/PostgreSQL/pgvector 和现有部署方式，不因计划调整更换技术栈。
5. 第 4 周完成后停止横向扩功能，把主要时间转向算法、后端基础、Agent/RAG 知识和实习投递。
6. 每周开始前只做一次实施 preflight：核对当前分支/基线、工作区、接口契约、迁移头和验证命令；不再重复开展大范围产品评审。

## 下一步第一件事

第 2 周 `LOOP-01` 至 `LOOP-06` 已完成离线、Mock 和隔离 PostgreSQL/pgvector 验收：下一步只可在审查后创建并合并学习闭环 PR；不开始第 3 周 UX、不执行新的真实 Provider 评测，也不部署 Render。
