# 7.1 工作分工文档

日期：2026-07-01

参与成员：陈、赵

## 1. 今日目标

今天的目标不是继续各自堆功能，而是在 `dev` 分支上完成第一次集成联调，确认 6.30 两个模块合并后能否跑通第一版主线。

当前主线：

```text
目标 -> 资料 -> AI 整理 -> 计划 -> 打卡 -> 进度
```

今天重点：

```text
1. 两人都切到最新 dev。
2. 启动后端并确认 /api/health 和 /docs 可访问。
3. 跑完整演示流程。
4. 记录联调问题清单。
5. 判断下一步是修 bug，还是进入资料与 AI 模块后端迁移。
```

暂不作为今天硬性任务：

```text
注册登录
PDF 上传
真实大模型接入
正式部署
直接合并 main
```

## 2. 今日分工原则

今天不再按“各自继续写自己的功能”推进，而是按集成验收推进。

```text
陈重点看：目标、计划、今日任务、打卡、进度、后端 API。
赵重点看：资料、AI 整理、闪卡、测试题、成长问答、资料模块迁移边界。
共同看：完整流程是否能演示，前后端边界是否清楚。
```

## 3. 陈今日任务

陈负责模块：

```text
目标管理 -> 行动计划 -> 今日任务 -> 打卡 -> 进度统计 -> 后端骨架
```

### 3.1 任务一：确认 dev 后端可运行

最低完成标准：

```text
能安装 backend/requirements.txt。
能启动 uvicorn。
/api/health 返回成功。
/docs 能打开。
```

### 3.2 任务二：检查目标和任务 API

重点检查：

```text
GET /api/goals
POST /api/goals
GET /api/tasks/today
POST /api/tasks/:id/checkin
GET /api/progress
```

最低完成标准：

```text
接口能在 /docs 中看到。
基础请求不报错。
返回格式符合 code / message / data。
```

### 3.3 任务三：确认前端目标流程

最低完成标准：

```text
用户能创建目标。
系统能生成任务。
今日任务能展示。
打卡后进度能变化。
刷新后数据状态符合预期。
```

## 4. 赵今日任务

赵负责模块：

```text
资料管理 -> AI 整理 -> 闪卡 / 测试题 -> 成长问答
```

### 4.1 任务一：确认资料模块在 dev 上可演示

最低完成标准：

```text
用户能添加资料。
系统能生成结构化整理结果。
资料详情能展示摘要、知识点、难点、学习顺序、行动建议。
```

### 4.2 任务二：确认闪卡和测试题关联

最低完成标准：

```text
闪卡有 materialId。
测试题有 materialId。
删除资料后，对应闪卡和测试题同步消失。
```

### 4.3 任务三：确认成长问答

最低完成标准：

```text
用户能提问。
回答能基于已有资料摘要或知识点。
AI 回答能显示参考资料。
刷新后问答记录不丢。
删除资料后问答引用被清理。
```

### 4.4 任务四：整理资料模块后端迁移边界

今天不要求直接完成后端迁移，但要写清：

```text
哪些对象要落库。
哪些 API 先做。
哪些继续 mock。
前端从 localStorage 切到 API 的顺序。
```

建议优先顺序：

```text
1. Material API
2. MaterialSummary / summarize mock service
3. Flashcard API
4. QuizQuestion API
5. AIConversation / ask API
```

## 5. 共同任务

两人共同完成：

```text
1. 确认 dev 是当前联调分支。
2. 跑完整演示流程。
3. 记录问题清单。
4. 给每个问题标记优先级。
5. 判断是否具备合并 main 的条件。
```

问题优先级建议：

```text
P0：主流程无法演示，今天必须修。
P1：影响体验或数据一致性，合 main 前要修。
P2：优化项，可以放到 MVP-1A / MVP-1B。
待确认：需要问导师或两人讨论。
```

## 6. 今日整体验收标准

今天结束时，至少能完成：

```text
1. 两人都能拉到最新 dev。
2. 后端能启动，/api/health 和 /docs 可访问。
3. 前端 app/index.html 可打开。
4. 能创建目标。
5. 能生成行动计划。
6. 能完成今日任务打卡。
7. 能看到进度变化。
8. 能添加资料。
9. 能生成结构化 AI 整理结果。
10. 能生成闪卡和测试题。
11. 能进行一次成长问答。
12. 删除资料后，闪卡、测试题、问答引用能同步清理。
13. 形成一份 dev 联调问题清单。
```

## 7. 陈模块验收标准

```text
1. 后端目标/任务/进度相关接口能启动并可访问。
2. 目标、计划、打卡、进度流程在前端或后端至少一条链路可演示。
3. 数据字段与 docs 中 Goal / PlanTask / CheckInRecord / Progress 基本一致。
4. 接口返回格式符合 code / message / data。
```

## 8. 赵模块验收标准

```text
1. 资料可以添加、查看、删除。
2. 资料整理结果结构化展示。
3. 闪卡和测试题通过 materialId 关联资料。
4. 删除资料时，对应闪卡和测试题同步删除。
5. 成长问答能基于已有资料给出 mock 回答。
6. 问答能展示参考资料。
7. 页面刷新后资料、摘要、闪卡、测试题和问答记录不丢失。
8. 能说清楚资料模块后端迁移的第一批 API。
```

## 9. 今日建议时间安排

```text
09:00 - 09:30 同步 dev 状态，确认今天只做联调验收。
09:30 - 10:30 启动后端，确认 /api/health 和 /docs。
10:30 - 12:00 跑第一遍完整演示流程，记录问题。
14:00 - 15:30 修 P0 / P1 问题或明确责任分工。
15:30 - 16:30 赵整理资料模块后端迁移边界。
16:30 - 17:30 两人复跑演示流程。
17:30 - 18:00 写今日小结和下一步。
```

## 10. 今日交付物

```text
1. dev 联调问题清单。
2. 完整演示流程是否通过的结论。
3. 赵模块后端迁移边界草案。
4. 是否可以合并 main 的判断。
5. 今日完成 / 未完成说明。
```

## 11. 明日可能衔接任务

如果 dev 演示通过：

```text
1. 准备合并 main。
2. 开始资料模块后端迁移。
3. 先做 Material API 和 summarize mock service。
```

如果 dev 演示不通过：

```text
1. 优先修 P0 主流程问题。
2. 暂缓合并 main。
3. 每人修自己模块的问题，再回到 dev 联调。
```
