# 当前状态

更新时间：2026-07-02

## 每天启动入口

新窗口开始时优先只读本文件，并检查：

```text
git status --short --branch
git log --oneline --decorate -n 5
```

除非本文件信息不够或与代码冲突，否则不要先读长文档。

## 当前阶段

```text
MVP-1B 前端接 API 基本推进到资料模块 CRUD 闭环，本地 dev 已包含当前成果，进入完整 MVP 验收、README/CI 和远端同步判断阶段。
```

当前更准确的定位：

```text
项目已经从 MVP-0 静态/localStorage 演示，推进到“可持久化学习助手 MVP”。
目标模块和资料模块后端均已接入 FastAPI + SQLite + pytest。
资料模块前端已经完成新增、编辑、删除、摘要、闪卡、测试题接后端 API。
下一步重点是完整流程验收、README/CI 准备和远端 dev 同步判断。
```

## 当前分支

```text
当前工作分支：dev
本地 dev 已 fast-forward 包含 feature/material-ai-memory 成果
```

当前本地 `dev` 最新提交为 `0081b37`，领先 `origin/dev` 11 个提交。
当前 `origin/dev` 仍停在 `6041122`。
当前 `origin/feature/material-ai-memory` 已到 `0081b37`。

最新重要提交：

```text
0081b37 docs: 更新MVP当前状态
f822d41 feat: 支持资料编辑接入API
550f6b3 feat: 接入资料模块前端API
6041122 Merge pull request #13 from chenwenv/feature/app-js-modules
9c1f1e8 docs: 添加MVP进度评估和7月2日安排
```

## 下一工作日主目标

```text
围绕本地 dev 当前成果推进 MVP 第一版稳定闭环。
重点不是继续新增大功能，而是确认完整主流程可演示、可持久化、可测试，并判断是否同步远端 dev。
```

优先顺序：

```text
1. 确认当前本地 dev 与 origin/dev / origin/feature/material-ai-memory 的关系。
2. 跑 pytest 和 smoke_api.py。
3. 跑完整 MVP 演示闭环。
4. 补 README、接口文档、验收清单和 CI 草案。
5. 和陈确认是否同步远端 dev。
6. dev 稳定后再判断 main 合并，暂不直接合 main。
```

## 陈负责

```text
1. 目标/计划/打卡/进度模块验收。
2. 检查目标模块前端是否真正接后端 API。
3. 跑 backend/tests 自动化测试和 smoke_api.py。
4. 补 README 启动、测试、SQLite 说明。
5. 准备 GitHub Actions CI 草案。
6. 设计后续“AI 调整学习计划”的接口草案。
```

## 赵负责

```text
1. 资料模块前端接后端 API 已完成新增、编辑、删除、摘要、闪卡、测试题链路。
2. 继续确认资料、摘要、闪卡、测试题刷新和重启后仍可从后端读取。
3. 维护资料模块前后端接口对照文档。
4. 明确当前 mock/rule-based AI service 和真实 AI 接入边界。
5. 配合完整 MVP 验收和远端 dev 同步判断。
```

## 已完成

```text
1. 陈负责模块已接入 SQLite：
   - goals
   - tasks
   - checkins
   - progress

2. 赵负责模块已接入 SQLite：
   - materials
   - material_summaries
   - flashcards
   - quiz_questions

3. 后端 API 已覆盖：
   - /api/goals
   - /api/tasks
   - /api/progress
   - /api/materials

4. 自动化测试已覆盖：
   - backend/tests/test_goal_flow.py
   - backend/tests/test_material_flow.py

5. 已生成并推送文档：
   - docs/status/MVP第一版进度评估与下一步计划.md
   - docs/daily/2026-07-02工作安排.md
   - docs/资料模块前后端接口对照.md

6. 资料模块前端已接入：
   - GET /api/materials
   - POST /api/materials
   - PUT /api/materials/{material_id}
   - DELETE /api/materials/{material_id}
   - summary / flashcards / quiz 生成和读回
```

## 已验证

```text
dev 分支已包含陈、赵两边后端和前端模块拆分基线。
本地 dev 已包含资料模块前端 API 接入和编辑闭环。
目标模块和资料模块后端均已进入 SQLite 持久化版本。
资料模块测试隔离机制已补充，避免测试污染正式数据库。
docs/status/MVP第一版进度评估与下一步计划.md 已记录当前 MVP 缺口。
docs/daily/2026-07-02工作安排.md 已明确下一工作日任务。
```

历史验证记录：

```text
python backend/smoke_api.py 曾验证可通过。
dev 前端完整演示曾通过：创建目标 -> 添加资料 -> AI 整理 -> 生成行动计划 -> 今日任务打卡 -> 查看进度 -> 闪卡/测试题 -> 成长问答。
演示截图：docs/status/dev-demo-smoke.png
```

下一次开始开发前仍需重新执行：

```text
python -m pytest backend/tests -v
python backend/smoke_api.py
```

## 当前未解决问题

```text
1. 资料模块 API 接入后仍需完整 MVP 主流程验收。
2. app/app.js 中仍可见 localStorage 逻辑，需要区分哪些是 fallback，哪些仍是核心数据来源。
3. 资料摘要、闪卡、测试题当前仍是 mock/rule-based AI，不是真实大模型能力。
4. 目标、资料、计划、打卡、进度之间的智能联动仍较弱。
5. README 还需要同步最新启动、测试、SQLite 和 CI 说明。
6. GitHub Actions CI 尚未实现。
7. 本地 dev 是否同步远端 origin/dev 需完整验收后再决定。
8. main 是否合入 dev 仍需 dev 完整稳定后再决定。
```

## 今日不做

```text
注册登录
PDF 上传
真实大模型接入
正式部署
向量数据库
完整 RAG
真正 Agent 循环
在 MVP 主流程验收未稳定前强行扩大功能范围
```

## 最新日文档

```text
docs/daily/2026-07-02工作安排.md
```

## 最新进度评估

```text
docs/status/MVP第一版进度评估与下一步计划.md
```

## 明日入口更新规则

每天结束时更新本文件的：

```text
更新时间
当前阶段
下一工作日主目标
陈负责
赵负责
已完成
已验证
当前未解决问题
最新日文档
```

