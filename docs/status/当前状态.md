# 当前状态

更新时间：2026-07-01

## 每天启动入口

新窗口开始时优先只读本文件，并检查：

```text
git status --short --branch
git log --oneline --decorate -n 5
```

除非本文件信息不够或与代码冲突，否则不要先读长文档。

## 当前阶段

```text
dev 完整演示已通过 + MVP-1A 前后端迁移准备
```

## 当前分支

```text
dev
```

## 今日主目标

```text
dev 完整演示已通过且暂无 P0；下一步开始目标/任务/打卡/进度前端接 API。
赵并行推进资料模块后端第一步：Material API。
```

## 今日第一步

```text
陈：从目标/任务/打卡/进度前端接 API 最小闭环开始。
赵：继续 Material API，并准备 summarize mock service。
```

## 陈负责

```text
1. dev 完整演示。
2. P0 / P1 问题清单。
3. 无 P0 后接目标、任务、打卡、进度 API。
```

## 赵负责

```text
1. Material schema。
2. materials router。
3. materials 表或 store 方法。
4. GET /api/materials。
5. POST /api/materials。
6. summarize mock service 作为下一步。
```

## 已验证

```text
python backend/smoke_api.py 可通过。
后端目标、计划、今日任务、打卡、进度 API 冒烟通过。
dev 前端完整演示已通过：创建目标 -> 添加资料 -> AI 整理 -> 生成行动计划 -> 今日任务打卡 -> 查看进度 -> 闪卡/测试题 -> 成长问答。
前端控制台无报错。
P0：暂无。
演示截图：docs/status/dev-demo-smoke.png
```

## 未解决问题

```text
1. 前端目标/任务/打卡/进度尚未正式接后端 API。
2. 赵模块资料/AI/闪卡/测试题/问答仍主要在前端 localStorage/mock。
3. 合 main 前需要确认是否接受当前 localStorage/mock 版本作为稳定演示版本。
```

## 今日不做

```text
注册登录
PDF 上传
真实大模型接入
正式部署
直接合 main
存在 P0 时继续做新 API 接入
```

## 最新日文档

```text
docs/daily/2026-07-01工作安排.md
```

## 明日入口更新规则

每天结束时更新本文件的：

```text
更新时间
当前阶段
今日主目标
今日第一步
已验证
未解决问题
最新日文档
```
