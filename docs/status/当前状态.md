# 当前状态

更新时间：2026-07-03

## 每天启动入口

新窗口开始时优先读取：

```text
LOCAL_TODAY.md
docs/status/当前状态.md
docs/planning/第二版项目计划书.md
```

并检查：

```text
git status --short --branch
git log --oneline --decorate -n 5
```

## 当前阶段

```text
第一版“可持久化学习助手 MVP”已完成验收，并已同步到 origin/dev。
当前进入第二版规划和开工阶段：在第一版学习闭环上，补上真实 AI 资料问答能力，并让它和目标、计划、记忆、进度主流程联动。
```

当前更准确的定位：

```text
项目已经从 MVP-0 静态/localStorage 演示推进到 FastAPI + SQLite 持久化版本。
目标模块和资料模块均已有后端 API、SQLite 持久化和 pytest 覆盖。
资料模块前端已经完成新增、编辑、删除、摘要、闪卡、测试题接后端 API。
dev 已合入 material_chunks、关键词检索和 RAG chunks 接口说明。
第二版优先沿用第一版两条业务链路推进，不按“纯前端/纯后端”重新切分。
```

## 当前分支

```text
当前工作分支：feature/v2-qa-frontend-integration
当前最新提交：eaaa36a feat: 接入资料问答历史前端闭环
origin/dev：已更新到 fa8243f Merge branch 'feature/material-ai-memory' into dev
feature/agent-ask-mock：已合入 dev，并已删除本地分支和远程分支
main：暂不直接合并，等待 dev 第二版阶段稳定后再判断
```

最新重要提交：

```text
fa8243f Merge branch 'feature/material-ai-memory' into dev
5c01c0a feat: 关联资料问答记录
eaaa36a feat: 接入资料问答历史前端闭环
03deb74 feat: persist agent QA records
64edca7 docs: 记录问答前端dev验收
c4b072f feat: 接入成长问答agent ask接口
cd2833f Merge pull request #15 from chenwenv/feature/agent-ask-mock
4d05ce3 feat: align agent ask response with v2 QA contract
b3e5955 docs: 记录资料片段闭环dev验收
```

## 第二版分工

第一版协作链路：

```text
陈：目标管理 -> 行动计划 -> 今日任务 -> 打卡 -> 进度统计
赵：资料管理 -> AI 整理 -> 闪卡 / 测试题 -> 成长问答
```

第二版延续关系：

```text
陈：目标推进链路 + AI 检索 / 问答后端底座
赵：资料学习链路 + AI 能力在资料学习流程里的落地
共同：问答 API、返回结构、目标上下文、资料不足规则、dev 集成和整体验收
```

## 赵负责

```text
1. 延续资料管理、AI 整理、闪卡、测试题和成长问答链路。
2. 维护资料录入、新增、编辑、删除、刷新读回等资料生命周期体验。
3. 在资料页接入 chunks 生成、chunks 读回和关键词搜索展示。
4. 在资料学习流程中接入 AI 问答入口。
5. 设计问答结果在前端的展示方式：答案、依据、建议、来源和资料不足提示。
6. 维护资料 / AI 模块接口对照文档。
7. 负责资料链路浏览器验收：资料能被整理、切分、检索，并进入后续问答流程。
```

## 陈负责

```text
1. 延续目标、计划、任务、打卡和进度主流程。
2. 维护已合入 dev 的 material_chunks、关键词检索和相关后端测试。
3. 继续推进 agent ask 或资料问答后端底座。
4. 负责检索 chunks、拼接上下文、调用真实大模型或 LangChain 问答链。
5. 负责问答记录后端存储方案和后端测试。
6. 评估 AI 建议如何与目标、计划和任务草稿联动。
7. 确保目标、计划、打卡、进度主流程在 dev 集成后不被破坏。
```

## 已完成

```text
1. 第一版 MVP 已验收通过：
   - 创建成长目标
   - 新增 / 编辑 / 删除资料
   - 生成资料摘要
   - 生成闪卡和测试题
   - 生成行动计划
   - 今日任务打卡
   - 查看进度
   - 刷新后数据仍在

2. 目标模块已接入 SQLite：
   - goals
   - tasks
   - checkins
   - progress

3. 资料模块已接入 SQLite：
   - materials
   - material_summaries
   - flashcards
   - quiz_questions
   - material_chunks

4. 后端 API 已覆盖：
   - /api/goals
   - /api/tasks
   - /api/progress
   - /api/materials
   - /api/materials/{material_id}/chunks
   - /api/materials/search

5. 自动化测试已覆盖：
   - backend/tests/test_goal_flow.py
   - backend/tests/test_material_flow.py

6. 关键文档已更新：
   - docs/status/MVP第一版验收记录.md
   - docs/status/第二版阶段验收记录.md
   - docs/planning/第二版项目计划书.md
   - docs/api/RAG-chunks接口说明.md
   - docs/modules/资料模块前后端接口对照.md
   - docs/README.md

7. 赵分支已完成第二版第一个资料学习链路闭环：
   - 资料总结区生成 chunks
   - 资料总结区读回并展示 chunks
   - 资料总结区关键词搜索 chunks
   - docs/modules/资料模块前后端接口对照.md 已补充 chunks 前端接入说明
   - 已合入 dev 并完成集成验收

8. 陈已完成并合入 agent ask mock 后端底座：
   - POST /api/agent/ask
   - 按 question 检索 chunks
   - 可按 goalId 过滤资料片段
   - 返回 answer、basis、suggestion、references、isFromMaterial、confidence、mode

9. 赵已完成并合入成长问答页 agent ask 前端接入：
   - app/api.js 新增 agentApi.ask()
   - 成长问答页提交问题调用 POST /api/agent/ask
   - 展示 answer、回答依据、学习建议、confidence、mode 和来源片段 references
   - 新增 docs/api/第二版AI问答接口交接.md
   - 已合入 dev 并完成集成验收

10. 赵已完成并合入资料学习链路中的问 AI 入口：
   - 资料总结区新增“问 AI”按钮
   - 点击后预填资料相关问题并切到成长问答页
   - 用户确认后仍通过成长问答页调用 POST /api/agent/ask
   - 资料入口发问时传入 materialId，支持后端保存到对应资料问答记录
   - 不改 agent ask 后端底座和问答历史持久化接口

11. 陈当前分支正在补齐 2026-07-03 问答前端联调收口：
   - app/api.js 新增 materialApi.listQaRecords(materialId)
   - app/app.js 初始化读取 GET /api/materials/{material_id}/qa
   - app/modules/materials.js 在资料总结区新增按资料提问入口
   - 提问成功后按 materialId 读回历史问答并展示 answer、basis、suggestion、sourceTitle、confidence
   - app/styles.css 补充资料问答历史展示样式
```

## 已验证

```text
第一版 MVP 主流程已通过验收。
python -m pytest backend/tests -v 曾通过。
python backend/smoke_api.py 曾通过。
dev 前端完整演示曾通过：创建目标 -> 添加资料 -> AI 整理 -> 生成行动计划 -> 今日任务打卡 -> 查看进度 -> 闪卡/测试题 -> 成长问答。
RAG chunks 基础能力已合入 dev，并有接口说明和测试覆盖。
赵分支 chunks 前端闭环已验证，并已在 dev 复跑：
- python -m pytest backend/tests -v：10 passed
- python backend/smoke_api.py：通过，包含 chunk_count / chunk_readback / chunk_search_count
- node --check app/api.js app/app.js app/modules/materials.js：通过
- Edge 浏览器 smoke：dev 上新增资料 -> 生成片段 -> 搜索 review tasks 命中，无控制台错误
成长问答页 agent ask 前端接入已在 dev 复跑：
- python -m pytest backend/tests -v：14 passed
- python backend/smoke_api.py：通过，包含 agent_mode / agent_reference_count / agent_from_material / agent_confidence
- node --check app/api.js app/app.js app/modules/chat.js app/modules/materials.js：通过
- Edge 浏览器 smoke：dev 上新增资料 -> 生成 chunks -> 成长问答提问 -> 展示回答依据、学习建议、来源片段和 confidence，无控制台错误
资料总结区问 AI 入口已在本地 Edge headless 验证：
- 新增目标和资料 -> 生成 chunks -> 点击资料总结区“问 AI” -> 自动切到成长问答页并预填问题
- 发送后展示回答依据、学习建议、confidence 和 3 条来源片段
- 传入 materialId 后，GET /api/materials/{material_id}/qa 可读回 1 条对应资料问答记录
- 控制台无 error / pageerror
资料问答历史前端接入当前分支已完成静态检查：
- node --check app/api.js app/app.js app/modules/chat.js app/modules/materials.js：通过
- python -m pytest backend/tests -v --basetemp .pytest_tmp_v2_qa_frontend：15 passed
- python backend/smoke_api.py：通过，包含 qa_record_count: 1
- python -m compileall backend：通过
```

下一次开始开发前建议重新执行：

```text
python -m pytest backend/tests -v
python backend/smoke_api.py
```

## 当前未解决问题

```text
1. 真实大模型 / LangChain 问答链尚未接入。
2. 资料问答历史前端入口仍需浏览器完整验收：资料提问 -> 刷新 -> 历史问答仍存在。
3. README 已同步当前 chunks / agent ask 能力；真实 AI 接入后仍需补充模型配置说明。
4. main 是否合入 dev 仍需 dev 第二版稳定后再判断。
```

## 今日不做

```text
注册登录
PDF 上传
正式部署
向量数据库
完整 RAG
真正 Agent 循环
LangChain memory
自动联网搜索
在问答交界面未确认前强行扩大功能范围
```

## 最新入口文档

```text
docs/planning/第二版项目计划书.md
docs/status/第二版阶段验收记录.md
docs/api/RAG-chunks接口说明.md
docs/api/第二版AI问答接口交接.md
docs/modules/资料模块前后端接口对照.md
docs/daily/2026-07-03工作安排.md
```

## 下一步第一件事

```text
先完成 feature/v2-qa-frontend-integration 与最新 origin/dev 的冲突解决、测试复跑和推送。
PR 合并到 dev 后，再进入真实大模型 / LangChain provider 抽象。
```
