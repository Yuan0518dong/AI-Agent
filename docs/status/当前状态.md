# 当前状态

更新时间：2026-07-22

## 产品经理评估与收口顺序

2026-07-22 评估结论：项目已具备可靠性、RAG、错题、FSRS、确认写入、可恢复 Runtime 和 Today Actions 等核心能力，当前瓶颈从“缺功能”转为“已有能力尚未形成一致、可复现、可求职交付的产品证据”。项目从功能开发切换到 7 至 10 天的求职交付收口，不改产品主题，不把“智能助手”改名为“教练”，不迁移 FastAPI/原生 JavaScript/SQLite/PostgreSQL/pgvector 技术栈，也不新增横向功能。

新的唯一执行顺序为：文档独立提交 -> `UX-04` 最小 P0 -> `UX-02` 文案/选中状态/ARIA -> 第 3 周总门禁 -> `CLOSE-02` 黄金流程 -> `CLOSE-01` 只修流程暴露缺口 -> `CLOSE-03` 三角色验收 -> `CLOSE-04` 指标与求职材料统一。每项独立启动、独立验证、独立本地提交；后续任务不得混入当前提交。

缺陷处理固定为：P0 阻断流程、数据/隔离/确认/安全问题必须清零；P1 只修黄金流程内且受时间盒限制；P2 只记录到维护清单。最终对外指标将由 `docs/evaluation/产品收口最终验收报告.md` 单一汇总，原有 A0/A2/A3、legacy/corrected、失败 Run 和各阶段报告继续保留且不得覆盖。远程推送、创建 PR、合并、部署和公开 Demo 远程复验仍等待单独授权。

2026-07-22 `UX-04` 已在当前本地分支完成：智能助手首层现在直接显示当前行动、原因、学习依据、运行状态、模式、Guard 和等待确认，复用既有 Run/Step 快照；技术信息继续默认折叠。`apply_confirmed_draft` 只提供接受/拒绝，`暂不处理`仅本地收起且零写入。新增定向 Playwright 为 `1 passed`，Agent 回归为 `55 passed`，390px/axe 和截图均通过；未实施 `UX-02`、新增 API、真实 Provider、推送、PR、合并或部署。下一项为 `UX-02` 实施前审计。

2026-07-22 `UX-02` 已在当前本地分支完成：桌面使用“今日、学习目标、资料、问答、智能助手、复习、进度”，移动使用“今日、目标、资料、智能助手、更多”；所有视图入口同步 `aria-current="page"`，移动隐藏页由“更多”的选中状态与当前页 ARIA 标签表达，菜单已关联控制按钮并支持 Esc 焦点回收。路由、视图 ID、导航数量和层级没有改变。新增定向 Playwright 为 `1 passed`，含零业务写入、390px/axe 与截图；未实施后端、数据库迁移、FSRS、真实 Provider、推送、PR、合并或部署。下一项为第 3 周总门禁。

2026-07-22 第 3 周总门禁已完成：完整 Mock 为 `200 passed, 6 skipped`，隔离 PostgreSQL/pgvector 为 `6 passed, 200 deselected`，迁移已实际执行 `upgrade head -> downgrade -1 -> upgrade head`；Ruff、compileall、pip check、敏感扫描和 diff check 均通过。完整 Playwright 为 `6 passed`，覆盖 Batch 4、UX-01 至 UX-04、桌面/390px、axe 与加载网络断言。门禁首轮暴露的重复智能助手控制和隔离 Demo 限流已在本周范围内修复，未发现 P0；详情见 `docs/evaluation/learning-loop-ux/week3-unified-gate-validation-report.md`。没有实施 `CLOSE-02`、真实 Provider、推送、PR、合并或部署。

2026-07-22 `CLOSE-02` 已完成：新增隔离 Mock Playwright `v7-close02.spec.cjs`，从新注册账户经目标、文本资料、显式生成来源片段、带引用问答、错误作答与薄弱点，到 Agent `create_review_draft` 的等待确认、接受写入、到期闪卡、FSRS `good` 评级和 Today 变化。测试直接验证确认前正式闪卡不变，接受后新增 1 张 `reviewCount=0/lastRating=null` 的正式卡并出现在到期队列；评级后为 `reviewCount=1/lastRating=good/status=known`，该卡从 Today 到期项移除。首轮发现接受后资料缓存未刷新，已在单独本地提交 `a786546` 只刷新已确认 `apply_confirmed_draft` 的资料状态；定向结果为 `1 passed`，390px、axe 与截图通过。完整证据见 `docs/evaluation/learning-loop-ux/close02-golden-flow-validation-report.md`；没有新增 API、迁移、真实 Provider、推送、PR、合并或部署。

2026-07-22 `CLOSE-01` 已完成：以黄金流程为基线复核既有 REL、Agent、学习闭环、Today 和用户隔离测试，未发现需要新增产品功能或迁移的缺口；确认后资料状态刷新的 P1 已保留在独立提交 `a786546`。定向 SQLite 回归为 `90 passed`；隔离 `pgvector/pgvector:pg16` 在 `127.0.0.1:55432` 实际完成 `upgrade head -> downgrade -1 -> upgrade head` 并回到 `20260721_03`，PostgreSQL 标记集为 `6 passed, 200 deselected`；全量 Mock 为 `200 passed, 6 skipped`，完整 Playwright 为 `7 passed`。Ruff、compileall、pip check、敏感扫描、前端语法和 diff check 均通过；容器已停止。完整命令、覆盖映射和失败分类见 `docs/evaluation/learning-loop-ux/close01-consistency-gate-validation-report.md`。没有真实 Provider、推送、PR、合并或部署。

2026-07-22 `CLOSE-03` 已完成：三条独立 Mock Playwright 固定角色验收为 `3 passed`。首次使用者完成注册、目标、资料、显式生成来源片段和带引用问答；错题学习者完成错误作答、复习草稿确认、正式闪卡、FSRS `good` 评级和 Today 移除；高风险确认/拒绝场景确认“暂不处理”零请求、拒绝零资料写入、取消终止 Run，且 Guard 保持未触发。三份截图均已人工复核；P0/P1/P2 均为 0，不将这组固定夹具表述为真实用户研究。证据见 `docs/evaluation/learning-loop-ux/close03-role-acceptance-report.md`。没有真实 Provider、推送、PR、合并或部署。

2026-07-22 `CLOSE-04` 已完成并冻结：新增 `docs/evaluation/产品收口最终验收报告.md` 作为当前对外指标唯一汇总来源，README、简历材料、面试稿、架构边界和 91 秒录屏说明均已回链。当前系统门禁为全量 Mock `200 passed, 6 skipped`、PostgreSQL/pgvector `6 passed, 200 deselected`、完整 Playwright `10 passed`；Ruff、compileall、pip check、敏感扫描、前端语法和 diff check 均通过。报告并列保留历史 A0/A2/A3 真实 Provider 失败基线与 REL corrected fixture，不把不同 fixture、Runtime 确定性决策或受控 Guard 注入写成模型能力提升。视频文件与既有 `00:01:31` 脚本已核对；它仍使用 Mock Provider，未完整展示的 FSRS/Today 流程以 CLOSE-02/CLOSE-03 证据补充。本次只修改文档，没有重跑真实 Provider、推送、创建 PR、合并、部署或远程 Demo 复验。

当前项目满足收口冻结条件并进入发布后维护状态：只处理新发现的 P0，以及时间盒内的黄金流程 P1；P2 只记录维护清单。产品主题、模块名称、技术栈与既有闭环保持不变。`SHIP-01` 至 `SHIP-03` 已完成；后续新增横向功能、付费真实 Provider 评测、生产部署变更或其他远程操作仍需单独授权。

2026-07-22 `SHIP-01` 已完成：从干净的 `feature/learning-loop-ux@9f9a509` 审计相对 `personal/main@9d2c59f` 的 14 个提交，确认范围只包含 UX、Today Actions、智能助手与导航收敛、黄金流程、一致性修复、三角色测试和最终材料。版本元数据已准备为 `7.1.0`。重新执行全量 Mock 为 `200 passed, 6 skipped in 141.27s`；隔离 `pgvector/pgvector:pg16` 完成 `upgrade head -> downgrade -1 -> upgrade head` 并回到 `20260721_03`，PostgreSQL 为 `6 passed, 200 deselected in 2.34s`；完整 Playwright 为 `10 passed in 49.6s`。Ruff、compileall、pip check、前端语法、敏感扫描和 diff check 均通过，P0 与阻断发布的 P1 为 0；临时容器已停止。PR 说明与 Release Notes 草案位于 `docs/evaluation/release-v7.1.0/`。没有真实 Provider、推送、PR、合并、标签、Release、部署或远程 Demo 操作。

2026-07-22 `SHIP-02` 已完成：preflight 确认干净工作区、`feature/learning-loop-ux@556be27`、`personal/main@9d2c59f` 祖先关系、`personal` 远程地址和 GitHub 认证；未向 `origin` 写入。已推送 `personal/feature/learning-loop-ux` 并创建 [PR #6](https://github.com/Yuan0518dong/AI-Agent/pull/6) 到 `personal/main`。Reliability checks、PostgreSQL pgvector integration 与 Playwright portfolio E2E 在 push 和 pull_request 两次触发中均为 `success`；PR 当前为 `OPEN/CLEAN`。没有合并、标签、Release、Render 部署、远程 Demo 复验或真实 Provider 评测。

2026-07-22 `SHIP-03` 已完成：PR #6 已以普通 merge commit [`09f6266`](https://github.com/Yuan0518dong/AI-Agent/commit/09f6266d1ffb32802106c92ec1a9370ea7e82f66) 合入 `personal/main`，父提交为 `9d2c59f` 和 `5b6f6a9`；`v7.1.0` 标签及 [GitHub Release](https://github.com/Yuan0518dong/AI-Agent/releases/tag/v7.1.0) 均精确指向该提交。主分支 CI [run 29898589032](https://github.com/Yuan0518dong/AI-Agent/actions/runs/29898589032) 的 Reliability checks、PostgreSQL pgvector integration 和 Playwright portfolio E2E 全部成功。Render Deploy Hook 部署为 `dep-d9g7gubtqb8s73b65d9g`，服务 Events 截图确认 `Live@09f6266`；随后公开 `/api/health` 实测 HTTP 200 / `status=ok`，Public Demo Smoke [run 29902464916](https://github.com/Yuan0518dong/AI-Agent/actions/runs/29902464916) 成功。公开实例的 Mock 黄金流程完成 1 个目标、1 份资料、1 个来源片段、规则决策 `create_task_draft` 等待确认与确认后仅 1 个草稿；FSRS `good` 后 `reviewCount=1`，队列 `2 -> 1`、Today 到期闪卡 `2 -> 1`、行动项 `5 -> 4`。首次手工核验错误使用不存在的 `flashcardId` 而收到 404，按 API 契约改用闪卡 `id` 后重跑通过；这是核验脚本字段错误，不是产品回归。本次未调用真实 Provider，未触碰 `origin`。

## 第 1 周 Agent 可靠性收口

2026-07-21 已在 `feature/agent-reliability-a3` 完成 `REL-01` 至 `REL-05`，未引入 FSRS、数据库迁移或产品界面改动。模型侧复习动作收敛为 `create_review_draft`：`materialIds` 为非空、去重、当前用户/目标范围内的必填列表；历史 `create_flashcards/create_quiz` 仅在 Guard 入口兼容并归一化，既有 AgentStep、ActionLog、A0/A2/A3 fixture 和报告均未迁移或改写。Runtime 会在明确复习草稿目标或存在测验薄弱点时，优先从 `quiz.weakAttempts[].materialId`、否则从当前目标资料确定性构造 `materialIds`；无法确定资料时改为安全的补资料建议，重做测试/检查掌握程度不创建测试草稿。

Guard 失败持久化为脱敏分类 `schema_invalid`、`disallowed_action`、`missing_required_field`、`payload_invalid`、`scope_invalid`，不保留模型原文。corrected rollback fixture 显式 `autoConfirm=true`，先创建一条完整和一条缺少 `front` 的 review draft；批量正式应用失败后，闪卡数保持 `0 -> 0`、两条草稿仍为 `confirmed`、ActionLog 仍为 `accepted`，验证第一条临时写入同样回滚。

离线门禁：REL 定向 `87 passed`；REL 双轨评测专项 `16 passed`；全量 Mock `182 passed, 4 skipped`；Ruff、compileall、pip check、敏感扫描、前端语法和 `git diff --check` 通过；PostgreSQL/pgvector `4 passed, 182 deselected`；Batch 4 Playwright（桌面、390px、axe）`1 passed`。`docs/evaluation/agent-reliability-rel/` 的报告分别记录 legacy fixture 与 A0/A2/A3 原始证据 SHA-256、corrected fixture 指纹、0 Provider 请求/Token/成本和 corrected 失败 Run；两套指标不连成同一趋势。

2026-07-21 已在用户明确授权（最多 60 个 Run、126 次请求、USD 0.27）内完成 corrected suite 真实 DeepSeek Provider 测评：60 个 Run 中 57 次 Provider 请求，prompt/completion Token 为 `94286/5553`，估算成本 `$0.014755`，p50/p95 为 `2094/4454 ms`。corrected 系统工具选择、确认完整性、恢复成功率和 Guard 注入召回均为 `1.0`，无失败 Run；Runtime 确定性 Decision 为 54 次，覆盖 36 个 Run，安全 fallback 为 17 次。`agent_case_016 × 3` 是 fixed `autoConfirm=true` 事务夹具，故本地执行且 0 Provider 请求；它验证回滚不变量，不能视为模型自然工具选择。Guard 指标来自真实 Provider 返回后的固定非法输出注入，也不代表模型自然非法输出率。完整逐 Run 安全摘要、fixture 指纹、请求、Token、成本和失败列表见 `docs/evaluation/agent-reliability-rel/corrected-real-provider-report.md`；该 corrected 集与 A0/A2/A3 legacy 集不同，指标不得接入历史曲线或表述为模型独立能力提升。

当前授权门：PR #3、PR #4 与学习闭环 PR #5 均已合并到 `personal/main`；PR #5 merge commit 为 `9d2c59f`，Reliability、PostgreSQL/pgvector 与 Playwright 主分支检查均通过。未部署或访问 Render。

## 第 2 周 错题与间隔复习

2026-07-21 已在 `feature/learning-loop` 完成 `LOOP-01` 至 `LOOP-06`（实现提交 `4637833`），没有实施 UX、部署或第 3 周工作。新增 MIT `fsrs==6.3.1`，固定 `desired_retention=0.9`、`maximum_interval=365`、禁用 fuzzing，并仅使用 UTC。`flashcards` 的 SQLite schema 与 PostgreSQL Alembic `20260721_03` 均加入 `fsrs_card`、`due_at`、`last_reviewed_at`、`review_count`、`last_rating` 与 `due_at` 索引；旧卡不信任原 status，统一以立即到期、未复习状态初始化，不伪造间隔。

唯一 `Flashcard Review Service` 和 `new_scheduled_flashcard()` 已接管资料批量生成、手工创建、Agent 确认、Demo 种子与历史状态接口。`fsrs_card` 是事实来源，每次评级在同一事务更新 JSON、到期投影、兼容状态和计数，并校验 `dueAt == Card.from_json(fsrsCard).due`；损坏 JSON 返回 `409 flashcard_schedule_invalid`，保留数据库原值。新增跨资料到期队列、四档评级与弱项接口；`known/review/new` 分别映射 Good/Again/仅未复习幂等，已复习卡拒绝重置。

薄弱点由既有 `quiz_attempts` 按 `quizId` 派生，错误或低于 70 分为未解决，之后正确且不低于 70 分自动解决；有任意 attempts 时重新生成测试题返回 `409 quiz_history_exists`，不删除原题或历史。Agent Context 已读取到期卡与未解决薄弱点，复习草稿仍保持确认后才正式写入，不新增测试题草稿。

本地验收：新增 LOOP 专项、资料/Agent 回归和全量 Mock 为 `196 passed, 6 skipped`；隔离 pgvector 容器经 Alembic `upgrade -> downgrade -> upgrade` 到 `20260721_03` 后标记集为 `6 passed`；Ruff、compileall、pip check、敏感扫描、前端语法、桌面/390px Playwright（含 axe）和 `git diff --check` 均通过。补强覆盖 FSRS 元数据一致性拒绝、并发评级防丢更新、显式 `409` 冲突、队列上限与稳定排序；CI 也会自动验证最新迁移回滚。完整口径、命令和环境限制见 `docs/evaluation/learning-loop/loop-offline-validation-report.md`。本轮 Provider 请求/Token/成本均为 `0`，不将离线结果表述为新的模型指标，也不覆盖 REL A0/A2/A3 或 legacy/corrected 证据。

## 第 3 周 学习闭环界面整合

2026-07-21 原 `UX-01` 提交 `e4b251e` 和记录提交 `a72aaaa` 曾把目标页改名为“学习空间”，并直接使用持久化 `selectedGoalId` 过滤资料、问答摘要、闪卡、测试和进度。2026-07-22 复核确认该方案改变了项目原生业务命名，使直接主导航隐式隐藏其他目标数据，也与 P1 仅做入口收敛的边界不一致。原提交、测试记录和 `docs/images/ux01-learning-space-mobile.png` 保留为被替代实现证据，不覆盖、不改写。

2026-07-22 已在当前 `feature/learning-loop-ux` 工作区完成 `UX-01` 纠偏：页面恢复“学习目标/目标详情”，五个目标快捷入口继续保留；新增非持久化临时目标范围，只有快捷入口设置范围，目标相关页面显示“当前目标 + 查看全部”；直接主导航清除临时范围但保留用户选中的学习目标，因此默认可见全部目标数据。刷新仍重新读取服务端目标并校验 `selectedGoalId`，失效选择会同时清除且不串目标。桌面和移动主导航结构未改，`UX-02` 至 `UX-04` 均未开始。

纠偏后的完整 Playwright 为 `2 passed in 16.1s`，包含既有 Batch 4 黄金流程和更新后的 UX-01；后者验证双目标、五入口、临时范围、查看全部、直接导航显示全部、刷新重校验、失效选择、只读请求和 390px 无横向溢出。新截图为 `docs/images/ux01-goal-shortcuts-mobile.png`。全量 Mock 为 `196 passed, 6 skipped in 133.17s`；Ruff、compileall、pip check、敏感扫描、前端语法和 `git diff --check` 通过。没有后端、迁移或 FSRS 改动，因此本次未重复 PostgreSQL/pgvector，仍以 `personal/main@9d2c59f` 已通过的精确基线为依据，第 3 周整体收口必须重跑。Provider 请求、Token 和成本均为 `0`；未推送、创建 PR、合并或部署 Render。

2026-07-22 已在同一工作区实现 `UX-03A` 至 `UX-03D`：今日页先渲染原 Dashboard，再以非持久化状态异步读取受限的 `/api/today/actions?limit=15`；该读取只投影逾期/今日任务、到期闪卡、未解决薄弱点和具有目标归属的 `proposed` 草稿，五类各最多 3 条、总数最多 15 条，不返回资料正文、chunks、Agent Context、Decision、时间线或草稿 payload。Today 日期切换不再读取完整 Agent Context；行动区有独立 loading/empty/error/retry，五类入口都按目标归属定位实体且不自动写入、问答或推进 Run。专项为 `4 passed`，全量 Mock 为 `200 passed, 6 skipped in 200.11s`，完整 Batch 4/UX-01/UX-03 Playwright 为 `4 passed`，桌面/390px 和 axe serious/critical=0；Ruff、compileall、pip check、敏感扫描、前端语法和 diff check 均通过。新截图为 `docs/images/ux03-today-actions-mobile.png`，完整记录见 `docs/evaluation/learning-loop-ux/ux03-today-actions-validation-report.md`。

随后已定位本机 Docker Desktop 并启动隔离 Linux engine，使用独立、自动删除的 `pgvector/pgvector:pg16` 容器在 `127.0.0.1:55432` 完成 Alembic `upgrade -> downgrade -1 -> upgrade head`，PostgreSQL 标记集为 `6 passed, 200 deselected`。此前 Docker daemon 不可用与旧专用测试连接认证失败的事实保留在验收报告中；它们没有导致迁移或外部数据库写入。UX-03 已满足本地离线门禁并本地提交 `0707f63`，工作区干净。`UX-02/04`、FSRS 调整、真实 Provider、推送、PR、合并与 Render 部署均未实施。

## 第七版进展

```text
2026-07-16 第七版 Batch 0 已完成本地验收：现有目标选择、中文化、答题草稿和侧边栏 UX 改动已复核；390px 登录后页面从 650px 文档宽度修复为 390px；Mock 基础门禁为 98 passed，compileall、pip check、前端语法检查和 diff check 均通过。
浏览器验收使用独立临时 SQLite 数据库和本地 Edge + Playwright，真实验证目标选择刷新保持、答题草稿重新渲染保持、桌面侧边栏吸顶以及七个已登录页面无文档级横向溢出，控制台和页面异常均为 0。
未跟踪 Dockerfile 已复核但未纳入 Batch 0：其 HuggingFace Spaces/7860/CORS=* 草稿与第七版 Render/Neon 部署约束不兼容，留待 Batch 1 重做。完整记录见 docs/status/第七版Batch0验收记录.md。
下一批只能从 Batch 0 验收提交创建本地 feature/v7-security-demo；本轮尚未推送、合并或修改远程仓库。
2026-07-16 第七版 Batch 1 的应用部署候选为 `46d62ec`，验证与 CI 修正提交均在 `personal/feature/v7-security-demo`；已按授权快进到 `personal/main`，未触碰 `origin`、未创建 PR。数据库 Cookie 会话、Argon2id/PBKDF2 升级、一键试用、IP/模型额度、同源 `/api`、CORS/Origin、防泄漏 Docker 上下文、Alembic/SQLAlchemy PostgreSQL 适配、pgvector CI 和公开 Demo smoke 工作流均已实现。Mock 全量回归为 `117 passed, 3 skipped`，Edge + Playwright 在隔离 SQLite 数据库中验证了访客种子、登出、注册刷新保持、HttpOnly 不可读、390px 和桌面布局。
隔离 Neon `batch1-test` 分支的 PostgreSQL 集成测试已再次为 `3 passed, 117 deselected`，覆盖登录、注册、Demo、LLM 接口的原子限流、会话哈希、pgvector、资料批量写入和并发额度预留；生产 Neon 分支已通过 Alembic 迁移到 `20260716_01`，实际读取到 `vector`、`users` 和池化运行 schema。GitHub Actions [run 29485625666](https://github.com/Yuan0518dong/AI-Agent/actions/runs/29485625666) 的 Reliability 和 PostgreSQL pgvector 两个 job 均成功。此前记录提交的首次 run 因浅克隆把历史空白问题纳入检查而失败，已通过 `fetch-depth: 2` 和 `HEAD^..HEAD` 范围检查修复，不影响已通过的 PostgreSQL 测试。
Render Docker Web 服务已上线至 [https://ai-agent-v7-yuan0518dong.onrender.com](https://ai-agent-v7-yuan0518dong.onrender.com)：`/`、`/api/health`、同源 Demo Cookie 会话和核心只读链路均已真实验收；Edge + Playwright 在公开 URL 验证一键试用、HttpOnly Cookie 不可读、登出、`390px`/`1440px` 布局、零应用 console/page error，非法 Origin 返回 `403 origin_forbidden`。Render 手动重新部署恢复 `Live` 后，部署前 Demo Cookie 仍成功读回会话和 1 个目标，随后已删除该 Cookie。公开实例刻意使用 Mock Provider，不把它表述为真实模型。
`PUBLIC_DEMO_URL` 已作为 GitHub Actions Variable 配置，`Public Demo Smoke` 已在默认分支 `main` 注册；手动 [run 29487956918](https://github.com/Yuan0518dong/AI-Agent/actions/runs/29487956918) 成功验证公开健康检查、Demo、Cookie 会话和目标只读链路，月度 cron 保留在同一工作流。2026-07-16 已完成本次求职季前的 Render/Neon 官方政策复核。Batch 1 的全部需求已验收；当前验收文档的独立收口提交完成后，才能创建 Batch 2 分支。已知运行风险：在确认 Render 可信代理 CIDR 前保持 `TRUST_PROXY_HEADERS=false`，IP 限流可能按 Render 代理而非真实访客聚合。详见 docs/status/第七版Batch1本地验收记录.md。
2026-07-17 第七版 Batch 2 已在本地分支 `feature/v7-ux-runtime` 完成验收：新增 Dashboard 首屏摘要、按视图延迟加载、服务端账户数据导出/删除、移动端底部五项导航与更多菜单，以及单 Step 的 `/api/agent/runs/{run_id}/advance`。智能任务默认仅保留目标、任务描述、快捷预设和开始按钮，`maxSteps`、Guard、Step、Tool I/O/JSON 与运行历史收纳在默认折叠的“运行详情”；`/execute` 仍保留给批处理和历史测试。
本地 Edge + Playwright 使用独立 SQLite 数据库实测新用户空状态、首屏仅 Dashboard 请求、目标/资料创建、逐步 advance、确认、取消、服务端导出、账户删除，以及全部七个视图在 `390px`、`768px`、`1440px` 下无文档级横向溢出。结果为 `1 passed`，截图见 docs/images/v7-batch2-desktop-runtime.png 和 docs/images/v7-batch2-mobile-navigation.png。Mock 全量门禁为 `123 passed, 3 skipped`；compileall、pip check、前端语法和 diff check 均通过。完整记录见 docs/status/第七版Batch2验收记录.md。本批没有推送、合并或修改远程仓库，且未进入 Batch 3。
2026-07-17 第七版 Batch 3 已在本地分支 `feature/v7-ingestion-rag` 完成验收：新增内存式 PDF/Markdown/TXT 上传、扩展名/MIME/签名与大小/页数/加密/OCR 安全校验、资料来源定位元数据、六阶段处理状态与单阶段重试；原始文件不落盘。链接资料在界面和服务端均明确为“仅保存链接，不解析网页正文”。
检索升级为关键词与 dense 各 Top 20、`k=60` RRF 融合，并在 dense 不可用时纯关键词降级；PostgreSQL Alembic `20260717_02` 增加 `vector(2048)`、来源字段和 cosine HNSW。Neon pgvector 的 `vector` HNSW 上限为 2000 维，索引因而使用 `embedding_vector::halfvec(2048)` 与 `halfvec_cosine_ops`，而 source vector 仍完整保存 2048 维。dense 检索先物化 HNSW Top 100 候选，再按原始 `vector(2048)` cosine 距离重排为 Top 20；SQLite 保留 JSON embedding。问答引用返回资料名、页码或 Markdown 标题/段落、原文、检索模式和分数；服务端没有充分引用时返回 `grounded-refusal`，不让模型常识冒充资料依据。Mock 全量门禁为 `137 passed, 4 skipped`；本地 Edge + Playwright 为 `1 passed`，在 `390px` 无文档横向溢出、页面异常和非预期控制台错误均为 0。截图见 docs/images/v7-batch3-desktop-ingestion.png 和 docs/images/v7-batch3-mobile-ingestion.png，完整记录见 docs/status/第七版Batch3验收记录.md。本批没有推送、合并或修改 `personal/main` 或 `origin`，且未进入 Batch 4。
2026-07-17 Batch 3 外部 Neon 验收已在同一隔离 endpoint 完成：`current` 从 `20260716_01` 经 direct Alembic `upgrade head` 到 `20260717_02`；实际确认 `vector` 扩展、`embedding_vector vector(2048)` 和 `idx_material_chunks_embedding_vector_hnsw` 的 `hnsw/halfvec_cosine_ops`。使用该隔离分支直连 URL 临时配置 `POSTGRES_TEST_DATABASE_URL`，mock LLM/embedding 下四项 PostgreSQL 集成测试分项均通过；全精度重排优化后的定向测试为 `1 passed, 3 deselected`，服务 SQL 可执行且 HNSW 查询计划生效。连接串、密码和 endpoint 标识均未写入仓库。本批外部收口完成，未进入 Batch 4。
2026-07-20 第七版 Batch 4 在本地分支 `feature/v7-evaluation-portfolio` 开始实施：新增 10 份固定中文资料、50 条检索查询和 32 条问答样例，分别运行 keyword/dense/hybrid 并输出逐条结果、指标和可复现数据集指纹。查询侧中文二字 n-gram 初始导致资料不足 F1=0.4；失败样例 `qa18` 至 `qa23` 已保留。修复查询侧三至四字 n-gram 与问句停用词后，离线 hybrid Recall@5=0.94，资料不足 Precision=1.0、Recall=0.75、F1=0.8571，`EVAL-04` 达到验收门槛。
20 个 Agent Mock 场景均通过，工具选择、Guard、确认完整性和可恢复确认的恢复成功率均为 1.0；Mock Token/成本均为 0。取得最多 126 次、`$0.27` 上限授权后，已配置 DeepSeek Provider 对 20 个固定场景各运行 3 次：60 个 Run 产生 107 次 Provider 请求、`237971/33611` prompt/completion Token，估算成本 `$0.042727`，预留成本 `$0.136668`。真实工具选择成功率为 `0.6333`、确认完整性为 `0.5556`、恢复成功率为 `1.0`，p50/p95 为 `5781/9750 ms`；22 个 Run 未满足场景断言，发生 84 次安全回退。Guard 召回 `1.0` 使用真实 Provider 响应后的固定非法输出注入，只证明 Guard 拦截链路，不代表自然非法输出率。真实运行器只覆盖 Agent 场景：创建 Run 的初始快照为规则决策，执行阶段才调用真实 `llm-json`，确认恢复读回已纳入预算，格式修复重试关闭。每请求发送前限制为 12,000 UTF-8 字节和 350 completion token；完整逐 Run 证据见 `docs/evaluation/第七版Batch4Agent真实模型评测报告.json`。
Batch 4 Playwright 使用隔离 SQLite 库验证一键试用、创建目标、PDF 上传、带引用问答、确认恢复、拒绝与取消；桌面和 390px 移动端通过，axe serious/critical=0、页面异常=0、非预期 console error=0。E2E 暴露并修复了新 Demo 会话保留旧 Agent 视图的加载竞态，现统一在“今日行动”落地；同时将辅助色调整为 WCAG AA 对比度。CI 已加入 Ruff、敏感信息扫描、Playwright 和 pgvector service job；在隔离直连 PostgreSQL 测试库迁移到 `20260717_02` 后，PostgreSQL 标记集实际为 `4 passed, 150 deselected in 251.68s`，pgvector schema/HNSW 重排定向测试为 `1 passed`。
README 已收敛为作品集入口，个人贡献、架构边界、90 秒演示脚本和 4 条简历材料已写入 `docs/portfolio/`。隔离 SQLite/Mock Provider 下的 Batch 4 演示已录制为 `docs/videos/v7-batch4-90s-demo.webm`，Windows 元数据时长为 `00:01:31`；README 明确该录屏不代表真实模型。`EVAL-05`、`EVAL-06` 已以受限真实运行和完整报告验收，`CI-01` 已由实际 PostgreSQL/pgvector 集成集验收。本批未推送、未合并、未创建 PR，未修改任何远程仓库。
2026-07-20 Batch 4 验收后补充完成第七版面试材料：`docs/planning/AI-Agent简历与面试讲解稿.md` 已统一为第七版架构、指标、真实模型限制、20 秒/1 分钟/3 分钟介绍、技术追问和 STAR 案例；`docs/portfolio/简历材料.md` 提供推荐 4 条、精简 3 条和面试披露边界。该补充作为独立本地提交，不改写 Batch 4 验收提交。
2026-07-20 第七版已发布到个人仓库 `personal/main` 并创建 `v7.0.0` Release；主 CI 的 Reliability、PostgreSQL/pgvector 和 Playwright 均通过。发布后的公开 Demo smoke 对 Render `/api/health` 连续超时，Release 已如实标注当前在线实例不可验收；恢复需要在 Render 控制台检查部署状态或手动重新部署后复跑。可靠性专项已从独立分支 `feature/agent-reliability-baseline` 开始：新增可复现失败分类工具，确认 22 个失败 Run 分布在 8 个场景，其中 7 个场景为 3/3 系统性失败；基线报告见 `docs/evaluation/真实Agent决策失败分类与可靠性实验基线.md`。
2026-07-20 可靠性 A1/A2 已在同一独立分支完成离线实现：Prompt 从底层 `availableTools` 改为与 Guard 一致的 `availableActions`，消除 `create_task_draft` 工具名与 `create_followup_tasks` 动作名等契约错位；新增 objective/Context/feedback-aware allowed-action policy，Prompt、Guard 和安全 fallback 使用同一动作集合。全量 Mock 门禁为 `160 passed, 4 skipped`，Ruff、compileall、pip check、前端语法、敏感扫描、diff check 与 Batch 4 Playwright 均通过。当前仍未执行新的真实 Provider 复跑，不得宣称指标提升；复跑预检要求再次明确授权最多 126 次请求、最高 `$0.27`。实现记录见 `docs/evaluation/真实Agent可靠性A1-A2实现记录.md`。
2026-07-20 用户明确授权最多 126 次请求、最高 `$0.27` 后，A2 使用相同 20 场景 × 3 次完成真实 DeepSeek 复跑：工具选择率 `0.75`、确认完整性 `0.6667`、恢复 `1.0`，15 个 Run 未满足断言；p50/p95 为 `3641/9796 ms`，93 次请求，`147390/28533` prompt/completion Token，估算成本 `$0.028624`。相较 A0，失败 Run 减少 31.8%、p50 降低 37.0%、成本降低 33.0%，但工具选择与确认仍未达到 `0.80/0.85`，Draft PR 继续不合并。A2 原报告的 fallback `27` 只统计 Step decision；12 个无 Step 安全终止 Run 的终态 decision 未被计入，故该值仅是下限，runner 已为后续评测修复。对比见 `docs/evaluation/agent-reliability-a2/A0-A2真实评测对比.md`。
2026-07-20 A3 在独立分支 `feature/agent-reliability-a3` 完成离线候选：当 policy 只剩一个可安全构造的动作时由 Runtime 直接生成带 `decisionPolicy` 的确定性 Decision，不调用模型；明确复盘草稿目标在创建草稿前不再开放 `answer_only`；模型输出 Schema 缩减为 `nextAction/reason/proposedActions(type,payload)`，其余状态由服务端补齐。全量 Mock 门禁为 `166 passed, 4 skipped`，Ruff、compileall、pip check、前端语法、敏感扫描、diff check 和 Batch 4 Playwright 均通过；固定20场景假 Provider 审计的执行决策请求由 A2 单次口径 29 降至 A3 的 21，最大请求包 `9065 < 12000` bytes，350 completion cap 全部保留。当前尚未真实复跑，不合并、不部署；设计与边界见 `docs/evaluation/真实Agent可靠性A3候选设计与离线验收.md`。
2026-07-20 用户授权继续执行后，A3完成相同20场景×3次真实DeepSeek复跑：系统工具选择率`0.8833`、确认完整性`0.7778`、恢复`1.0`，7个Run未满足断言；p50/p95为`2219/4750 ms`，67次Provider请求，`109995/7400` prompt/completion Token，估算成本`$0.017471`。Runtime确定性Decision为41次、覆盖32个Run，故该工具选择率是系统指标而非纯模型指标。`003/008/018/019`均提升到3/3；`004`与`014`各剩2次Guard fallback；`016`因无draft ID且tool_failure类别不自动确认仍为0/3。工具选择达到0.80门槛，确认未达到0.85，A3继续保持Draft、不合并、不部署。对比见`docs/evaluation/agent-reliability-a3/A0-A2-A3真实评测对比.md`。
```

## 每天启动入口

新窗口开始时优先读取：

```text
LOCAL_TODAY.md
docs/status/当前状态.md
docs/planning/个人项目简历化与第四版智能体方案.md
docs/planning/第五版可控学习智能体升级计划书.md
```

并检查：

```text
git status --short --branch
git log --oneline --decorate -n 5
```

## 当前阶段

```text
第一版”可持久化学习助手 MVP”已完成验收，并已同步到 origin/dev。
第二版已经完成最终收口；当前进入第三版”可控学习智能体 MVP”推进阶段。
第三版不再按大而全 Agent 扩张，而是优先把已有学习链路做成可演示、可验证、可解释的智能体闭环。
2026-07-07 后，项目进入个人作品集化和第四版完整学习智能体闭环规划阶段。
2026-07-08 第五版全部功能包已完成并推送 personal/dev 和 personal/feature/v3-agent-action-entry：
AgentRun / Tool Registry / LLM JSON Decision hybrid / Feedback Memory / Decision Guard / 第五版验收样例 / 简历面试讲解稿。
第五版验证：LLM_PROVIDER=mock pytest backend/tests 53 passed；smoke 通过含 agent_hybrid_guard_status=fallback；node --check 通过。
2026-07-09 Decision Guard 前端可观察化和浏览器截图证据已完成，README 已加入 hybrid fallback Guard 面板截图。
语义检索升级任务已经从“下一步”转为已完成基线，后续以第六版主需求文档为准。
2026-07-15 语义检索第一阶段已完成：SQLite JSON embedding、cosine similarity Top-K、关键词 fallback 和 MockEmbeddingProvider 测试基线。
2026-07-15 项目定位纠偏：第五版属于可控决策工作流，第六版开始实现真正的多步 Agent Runtime。
第六版第一批已完成：objective、持久化 AgentStep、工具执行、observation 回灌、确认暂停/恢复，以及 completed / failed / max_steps 停止条件。
当前验证：LLM_PROVIDER=mock、EMBEDDING_PROVIDER=mock 时 backend/tests 98 passed；Mock smoke 覆盖 waiting_confirmation、confirmed apply、正式任务写入、预算耗尽 readback、重复写入去重和 Guard fallback；Batch D 20/20 场景评测、compileall、前端语法检查、pip check、git diff --check 均通过。
第一个真实领域工具闭环已完成：review_material 会补齐缺失 chunks/embeddings 和摘要，下一轮 Context 能读回处理结果并改变 Decision。
search_materials 真实工具已完成：支持语义 Top-K、goal/material 范围过滤，并把 references 数据回灌下一次 Decision。
answer_with_sources 真实工具已完成：复用统一 grounded answer 服务，回答结果和 references 会持久化为 QA 记录并回灌下一步 Decision。
Batch B4/B5 已完成：统一 agent_drafts、幂等创建、查询隔离、确认状态推进、事务正式写入和重复恢复去重均已有测试证据。
Batch B6 已完成：确认写入后无条件读取原 Run 用户和目标范围内的最新 Context / Decision，并在预算耗尽前持久化；task/review、失败保留、隔离和重复恢复均已有专项测试。
Batch C 已完成：真实 OpenAI-compatible Embedding、严格 JSON/范围 Guard、一次格式修复、上下文裁剪、Provider 元数据、终态 Reflection 和真实 Provider 六类记录均已验收。
Batch D 已完成：20 个固定场景的 Mock Runtime 评测为 20/20 通过；真实 embedding-3 评测中语义检索 Recall@3=1.0、MRR=0.8333，均高于关键词 0.6667。
Batch E 已完成：前端 AgentStep 时间线展示 Decision、Guard、工具 Input/Output、ActionLog、错误、历时和 stopReason；确认步骤可接受并恢复原 Run，也可拒绝并安全终止。真实 Provider 成功、Guard 拦截、确认恢复、拒绝确认和 Provider 不可达回退均有浏览器证据；演示数据可由 `python backend/init_batch_e_demo_data.py` 幂等初始化。
Batch F 已完成：Run 新增 `cancelled` 终态和取消接口；SQLite 条件更新保证同一 Run 仅一个 executor 推进；读工具 timeout 最多重试一次、写工具不盲重试；AgentRun/Step/ActionLog 持久化脱敏。固定依赖、CI、同源单命令启动、端到端录屏和最终 API/Tool/架构文档已验收。第六版全部完成。
```

当前更准确的定位：

```text
AI-Agent 是实习期间个人主导的 AI 学习智能体项目，可作为后续简历和作品集基础。
项目已经从 MVP-0 静态/localStorage 演示推进到 FastAPI + SQLite + OpenAI-compatible Provider 的可演示版本。
当前已具备目标、任务、资料、chunks、资料问答、复习草稿、闪卡、测试批改、进度统计和可观察 Guard 回退等基础闭环。
第三版完成了可控学习智能体 MVP：nextAction、requiresConfirmation、insufficiencyReason、reviewDrafts，以及真实模型三类样例验收。
第四版已经补齐 AgentContext、AgentDecision、AgentActionLog、草稿态执行、任务草稿和上下文回读验收。
Batch D 已验收：评测报告包含任务成功、Guard 召回、恢复、重复写入、工具错误、步数、超限和检索指标；评测报告不记录 Key 或 Authorization。Batch E 已验收：AgentRun 前端可观察化、真实模型浏览器成功、Guard/Provider fallback、确认恢复和简历/演示材料均已收口。Batch F 已验收：可靠性、复现交付和最终契约均已收口；第六版全部完成。
```

## 当前分支

```text
当前工作分支：feature/v7-ingestion-rag
当前 Batch 3 基线：44677f6 feat: complete v7 batch 2 ux runtime；Batch 3 已在本地分支独立收口，不推送、不合并。
当前个人远程：personal -> https://github.com/Yuan0518dong/AI-Agent.git
personal/feature/v7-security-demo：包含 Batch 1 部署候选、验收记录和 CI 修正提交
personal/main：已按授权快进到 Batch 1 当前验证提交，公开 smoke 已从默认分支运行
personal/dev：已同步到 4b5c5fa
personal/feature/v3-agent-action-entry：已同步第六版完成提交
origin：仍保留原远程 chenwenv/AI-Agent，不作为个人作品集主远程
第六版语义检索、Agent Runtime、真实工具、评测、前端可观察化、可靠性/CI/交付、截图、录屏、演示材料和需求文档已统一收口到功能分支。
Batch 1 的 Dockerfile、.dockerignore、Alembic、SQLAlchemy/PostgreSQL 适配和部署工作流已进入 46d62ec；本轮仅保留最终验收文档勾选的工作区修改，不上传运行日志、真实 Key、Cookie 或本地数据库。
```

最新重要提交：

```text
b3bd7ca docs: add Guard screenshot and semantic search plan
4b5c5fa feat: add Decision Guard observability in agent workbench
d01e5f5 fix: add GitHub Pages URL to CORS origins
e25a28e deploy: add Render config, fix CI, update CORS and docs
2c72ed1 feat: add agent decision guard
```

## 第二版分工

第一版协作链路：

```text
陈：目标管理 -> 行动计划 -> 今日任务 -> 打卡 -> 进度统计
赵：资料管理 -> AI 整理 -> 闪卡 / 测试题 -> 成长问答
```

第二版延续关系：

```text
陈：目标推进链路 + AI 检索 / 问答后端底座
赵：资料学习链路 + AI 能力在资料学习流程里的落地
共同：问答 API、返回结构、目标上下文、资料不足规则、dev 集成和整体验收
```

## 赵负责

```text
1. 延续资料管理、AI 整理、闪卡、测试题和成长问答链路。
2. 维护资料录入、新增、编辑、删除、刷新读回等资料生命周期体验。
3. 在资料页接入 chunks 生成、chunks 读回和关键词搜索展示。
4. 在资料学习流程中接入 AI 问答入口。
5. 设计问答结果在前端的展示方式：答案、依据、建议、来源和资料不足提示。
6. 维护资料 / AI 模块接口对照文档。
7. 负责资料链路浏览器验收：资料能被整理、切分、检索，并进入后续问答流程。
```

## 陈负责

```text
1. 延续目标、计划、任务、打卡和进度主流程。
2. 维护已合入 dev 的 material_chunks、关键词检索和相关后端测试。
3. 继续推进 agent ask 或资料问答后端底座。
4. 负责检索 chunks、拼接上下文、调用真实大模型或 LangChain 问答链。
5. 负责问答记录后端存储方案和后端测试。
6. 评估 AI 建议如何与目标、计划和任务草稿联动。
7. 确保目标、计划、打卡、进度主流程在 dev 集成后不被破坏。
```

## 已完成

```text
1. 第一版 MVP 已验收通过：
   - 创建成长目标
   - 新增 / 编辑 / 删除资料
   - 生成资料摘要
   - 生成闪卡和测试题
   - 生成行动计划
   - 今日任务打卡
   - 查看进度
   - 刷新后数据仍在

2. 目标模块已接入 SQLite：
   - goals
   - tasks
   - checkins
   - progress

3. 资料模块已接入 SQLite：
   - materials
   - material_summaries
   - flashcards
   - quiz_questions
   - material_chunks

4. 后端 API 已覆盖：
   - /api/goals
   - /api/tasks
   - /api/progress
   - /api/materials
   - /api/materials/{material_id}/chunks
   - /api/materials/search

5. 自动化测试已覆盖：
   - backend/tests/test_goal_flow.py
   - backend/tests/test_material_flow.py

6. 关键文档已更新：
   - docs/archive/status/MVP第一版验收记录.md
   - docs/archive/status/第二版阶段验收记录.md
   - docs/archive/planning/第二版项目计划书.md
   - docs/api/RAG-chunks接口说明.md
   - docs/modules/资料模块前后端接口对照.md
   - docs/README.md

7. 赵分支已完成第二版第一个资料学习链路闭环：
   - 资料总结区生成 chunks
   - 资料总结区读回并展示 chunks
   - 资料总结区关键词搜索 chunks
   - docs/modules/资料模块前后端接口对照.md 已补充 chunks 前端接入说明
   - 已合入 dev 并完成集成验收

8. 陈已完成并合入 agent ask mock 后端底座：
   - POST /api/agent/ask
   - 按 question 检索 chunks
   - 可按 goalId 过滤资料片段
   - 返回 answer、basis、suggestion、references、isFromMaterial、confidence、mode

9. 赵已完成并合入成长问答页 agent ask 前端接入：
   - app/api.js 新增 agentApi.ask()
   - 成长问答页提交问题调用 POST /api/agent/ask
   - 展示 answer、回答依据、学习建议、confidence、mode 和来源片段 references
   - 新增 docs/archive/api/第二版AI问答接口交接.md
   - 已合入 dev 并完成集成验收

10. 赵已完成并合入资料学习链路中的问 AI 入口：
   - 资料总结区新增“问 AI”按钮
   - 点击后预填资料相关问题并切到成长问答页
   - 用户确认后仍通过成长问答页调用 POST /api/agent/ask
   - 资料入口发问时传入 materialId，支持后端保存到对应资料问答记录
   - 不改 agent ask 后端底座和问答历史持久化接口

11. 资料问答历史前端读回已完成：
   - app/api.js 新增 materialApi.listQaRecords(materialId)
   - app/app.js 初始化读取 GET /api/materials/{material_id}/qa
   - app/modules/materials.js 在资料总结区新增按资料提问入口
   - 提问成功后按 materialId 读回历史问答并展示 answer、basis、suggestion、sourceTitle、confidence
   - app/styles.css 补充资料问答历史展示样式

12. LLM Provider 抽象已完成：
   - 新增 backend/app/services/llm_provider.py
   - agent_service.py 继续负责 chunks 检索、goal/material 过滤和统一返回结构
   - MockLLMProvider 负责当前 mock 回答生成
   - get_llm_provider() 预留后续按环境变量切换真实大模型 Provider
   - 新增 docs/api/LLM-provider设计说明.md

13. 资料历史问答复盘入口已完成本地闭环：
   - 资料问答历史每条记录新增“转闪卡草稿”和“记复习点”
   - 记忆页新增“复盘草稿”区域，展示从 QA 生成的闪卡草稿和复习点
   - 同一条 QA 同一类型草稿去重，支持删除草稿
   - 删除资料时同步清理本地复盘草稿，避免留下孤儿数据
   - 当前先落在前端 localStorage 草稿，不新增后端表或 QA 生成闪卡 API

14. 第二版公共验收文档已收口：
   - docs/archive/planning/第二版项目计划书.md 补充 2026-07-03 当前进度
   - docs/archive/status/第二版阶段验收记录.md 更新四个已验收小闭环
   - docs/archive/api/第二版AI问答接口交接.md 更新历史问答和复盘入口状态
   - docs/archive/status/第二版验收样例与演示脚本.md 新增三类问题样例和演示脚本
   - README.md 和 docs/README.md 同步入口与当前限制

15. OpenAI-compatible LLM Provider 入口已新增：
   - backend/app/services/llm_provider.py 新增 OpenAICompatibleLLMProvider
   - 通过 LLM_PROVIDER、LLM_API_KEY、LLM_MODEL、LLM_BASE_URL 配置真实模型服务
   - 支持自动读取 backend/.env；backend/.env.example 提供模板，真实 backend/.env 不提交
   - 配置缺失、调用失败或返回格式不可解析时自动回退 MockLLMProvider
   - backend/tests/test_llm_provider.py 增加 Provider 选择和 JSON 响应映射测试
   - README.md 和 docs/api/LLM-provider设计说明.md 已补充配置说明

16. 真实模型人工联调分支已创建：
   - 当前分支 feature/real-llm-smoke
   - 新增 docs/archive/status/真实模型联调记录.md
   - 目标是用真实 API Key 验证 OpenAI-compatible Provider 的三类问题表现
   - API Key 只通过环境变量配置，不写入代码或文档
   - 智谱 glm-4-flash-250414 已完成三类问题直连 Provider 联调
   - 针对 LangChain 不在资料中但模型常识补全的问题，Provider 已增加技术词缺失防护

17. 闪卡复习闭环已补完：
   - 复盘草稿支持用户确认后“加入闪卡”
   - 新增 POST /api/materials/{material_id}/flashcards/custom 保存单张闪卡
   - 新增 PATCH /api/materials/{material_id}/flashcards/{flashcard_id} 保存 known / review 状态
   - 记忆页展示当前第几张、总数和卡片状态
   - 资料问答 -> 复盘草稿 -> 正式闪卡 -> 复习打分 已形成可刷新读回的后端持久化闭环

18. 第二版最终收口已完成：
   - 新增 docs/archive/status/第二版最终验收记录.md
   - dev 已包含闪卡复习闭环和远程登录隔离扩展
   - 最终验收命令通过：pytest 33 passed、smoke 通过、node --check 通过、compileall 通过
   - 当前建议进入 dev -> main 合并判断，不继续扩大第二版功能范围

19. 基础登录注册和用户数据隔离已由远程 dev 合入：
   - 新增 auth router / auth service / auth tests
   - 新增用户隔离测试
   - 合入后最终验收复跑通过：pytest 33 passed

20. 2026-07-06 本地新增 AI 学习能力：
   - 目标行动计划生成已接入 AI 优先路径，失败时回退 mock 计划
   - 资料测试题生成已接入 AI 优先路径，失败时回退 mock 题目
   - 测试题提交后支持 AI 批改，返回 isCorrect、score、feedback、suggestion、mode
   - 前端记忆页测试题从“只看参考答案”升级为“填写答案 -> 提交批改 -> 展示反馈”
   - 批改兜底模式已修正：真实模型未返回有效结果时显示 mock，不再误标 openai-compatible

21. 2026-07-06 本地修复 LLM 配置和真实模型访问链路：
   - backend/app/services/llm_provider.py 支持根目录 .env 和 backend/.env 两种位置
   - 支持 LLM_PROVIDER=zhipu / ZHIPU_API_KEY / ZHIPU_BASE_URL / ZHIPU_MODEL 配置方式
   - 智谱 glm-4-flash 连通性已验证，真实网络权限下返回 status 200
   - Codex 沙箱内启动的后端无法访问外网时会降级 mock；用户本机正常终端启动后端可访问智谱
   - API mode=openai-compatible 已通过本地接口验证

22. 2026-07-06 本地升级资料总结：
   - backend/app/services/material_ai_service.py 从本地模板总结升级为 AI 优先总结
   - 资料总结返回 overview、keyPoints、difficulties、studyOrder、actionItems
   - difficulties 要求分析真正可能卡住的理解点，不再简单重复原文句子
   - 请求模型时增加 response_format=json_object，并兼容 keyPoints/key_points 等字段命名
   - 如果没有解析到有效 AI 总结字段，则保持 mock 标记，避免假装是真实模型总结
   - 测试环境显式设置 LLM_PROVIDER=mock，避免 pytest 依赖外网和真实 key

23. 2026-07-06 本地优化前端资料学习信息架构：
   - 成长问答成为唯一 AI 对话入口
   - 资料总结区移除独立“资料问答”输入框，避免与成长问答重复
   - 资料总结区保留“围绕此资料提问”快捷按钮，点击后将 materialId 和预设问题带入左侧成长问答
   - 资料总结区“资料问答”改为“关联问答记录”，只负责展示该资料相关历史记录
   - 首页文案从“资料问答”调整为“资料理解”

24. 2026-07-06 本地修复资料模块前端乱码：
   - 修复 app/modules/materials.js 中因编码写入导致的中文乱码
   - 修复按钮和标签中出现的残破 HTML 文本
   - 清理 UTF-8 BOM 残留
   - 资料、片段、问答记录、复盘草稿等文案恢复为正常中文

25. 第三版最小学习智能体决策字段已合入 dev：
   - POST /api/agent/ask 返回 nextAction、requiresConfirmation、insufficiencyReason、reviewDrafts
   - QA 历史持久化并读回上述 Agent 决策字段
   - 当前资料泛化提问会回退使用当前资料 chunks，避免误判“资料不足”
   - 资料外问题仍会返回资料不足判断
   - 测试题生成从泛化“核心含义”优化为解释题、应用题和依据/边界题

26. 陈的第三版今日/进度页优化已合入 dev：
   - 今日页新增学习看板、主线目标、今日重点和进度环
   - 进度页新增总览、风险目标、复盘建议和目标进度分区
   - 目标计划生成进一步过滤不相关资料，避免把无关资料写进行动计划

27. 赵的第三版资料学习智能体闭环已合入并推送 dev：
   - 成长问答新增“智能体验收”入口，内置资料内回答、复习建议、资料不足三类样例
   - 样例会自动绑定当前资料并填入 Agent 问答输入框
   - Agent 回答后立即同步到右侧资料问答历史，并高亮当前记录
   - 资料问答历史可直接“采纳 Agent 草稿”
   - 采纳后自动进入复盘草稿区，并高亮当前草稿
   - 复盘草稿可继续加入正式闪卡，形成“样例问题 -> Agent 回答 -> 历史问答 -> 草稿确认 -> 正式闪卡”闭环
   - 新增 docs/status/第三版资料学习智能体验收样例.md 作为验收证据清单

28. 2026-07-07 第四版 AgentContext 只读接口已落地：
   - 新增 backend/app/services/agent_context_service.py
   - 新增 GET /api/agent/context，支持按 goalId 聚合单目标上下文，也支持全局学习上下文
   - AgentContext 当前读取目标、任务、资料摘要、chunks 数量、资料问答、资料不足记录、闪卡状态、测试题尝试和进度统计
   - 返回 summary.observations，供后续“智能体工作台”展示 Agent 看见的状态
   - 当前接口只读，不自动重排任务、不自动写入闪卡或复习草稿
   - backend/smoke_api.py 已加入 agent_context_* 关键计数

29. 2026-07-07 第四版智能体工作台最小展示已落地：
   - app/api.js 新增 agentApi.getContext()
   - app/modules/agent-workbench.js 新增工作台渲染模块
   - app/index.html 新增“智能体”导航和 view-agent 页面
   - 工作台展示 AgentContext 的观察摘要、任务风险、资料状态、复习队列、测试薄弱点和资料不足记录
   - 当前仍为只读展示，不自动写库；用户可从工作台跳转到目标、资料、记忆、学习和进度页面
   - 目标/资料/问答/打卡/闪卡状态变化后会刷新 AgentContext 缓存

30. 2026-07-07 第四版 AgentDecision 只读决策接口已落地：
   - 新增 backend/app/services/agent_decision_service.py
   - 新增 POST /api/agent/decide，支持按 goalId 生成单目标决策，也支持全局决策
   - 决策当前为 rule-based，不依赖真实模型，基于 AgentContext 判断 missing_goal、overdue_tasks、missing_chunks、missing_summary、material_insufficiency、weak_quiz_attempts、review_queue、missing_tasks
   - 返回 stateSummary、problems、nextAction、reason、requiresConfirmation、proposedActions
   - app/api.js 新增 agentApi.decide()
   - 智能体工作台新增“生成建议”按钮和建议展示区，展示发现的问题与待确认动作
   - 当前仍为只读建议，不自动执行重排任务、生成草稿或写库

31. 2026-07-07 第四版 AgentActionLog 最小反馈闭环已落地：
   - 新增 agent_action_logs SQLite 表
   - 新增 backend/app/services/agent_action_log_service.py
   - 新增 GET /api/agent/action-logs、POST /api/agent/action-logs、PATCH /api/agent/action-logs/{log_id}
   - 支持 status: proposed / accepted / rejected / later / applied
   - 前端工作台 proposedActions 新增“采纳 / 忽略 / 稍后”反馈按钮
   - 反馈会写入后端 ActionLog，并在工作台“反馈记录”区域读回展示
   - 当前仍只记录反馈，不自动执行任务重排、复习草稿或闪卡写入

32. 2026-07-07 第四版 ActionLog applied 状态闭环已补齐：
   - 后端 PATCH /api/agent/action-logs/{log_id} 已验证支持 applied 状态
   - smoke 中覆盖 accepted -> later -> applied 状态流转
   - 前端工作台“反馈记录”区域新增“标记已执行”按钮
   - 用户可将已采纳、忽略或稍后处理的建议标记为 applied
   - 当前 applied 仍只代表用户确认已处理，不自动触发任务重排或内容写库

33. 2026-07-07 第四版低风险 proposedAction 执行入口已补齐：
   - 前端工作台“标记已执行”升级为“执行并标记”
   - review_material / ask_for_more_material 跳转资料页
   - create_flashcards / create_quiz 跳转记忆页
   - reschedule_tasks / create_followup_tasks 跳转目标页
   - answer_only 跳转学习问答页
   - 跳转后同步把对应 ActionLog 标记为 applied
   - 仍不自动重排任务、不自动写入复习草稿或闪卡

34. 2026-07-07 第四版草稿态执行已补齐：
   - create_flashcards / create_quiz 执行后先生成本地复盘草稿
   - 复盘草稿复用现有“确认后加入正式闪卡”流程，不直接写正式闪卡
   - ask_for_more_material 执行后预填资料表单，用户确认保存后才写入资料库
   - create_followup_tasks / reschedule_tasks 仍只跳转目标页，不自动改任务表
   - ActionLog 继续记录 applied，表示用户已进入处理流程
   - 这一阶段完成了“建议 -> 用户反馈 -> 草稿态执行 -> 用户确认写入”的低风险闭环

35. 2026-07-07 第四版任务草稿态执行已补齐：
   - app/app.js 新增 agentTaskDrafts 本地状态
   - create_followup_tasks / reschedule_tasks 执行后生成目标任务草稿
   - 目标详情页新增 Agent 任务草稿面板，展示建议天数和任务预览
   - 用户可删除草稿，或点击“按草稿生成计划”进入现有计划生成确认流程
   - 仍不直接写 tasks 表；正式写库继续复用目标计划生成的确认弹窗
   - 任务草稿生成后 ActionLog 标记 applied，表示建议已进入处理流程

36. 2026-07-07 第四版上下文回读验收已补齐：
   - 复盘草稿加入正式闪卡后，前端会重新刷新 AgentContext
   - 新增测试证明自定义闪卡写入后，下一轮 AgentContext.summary.flashcardTotal 会增加
   - 新增测试证明下一轮 AgentDecision 能看到 review_queue 问题
   - smoke 输出 agent_context_flashcard_before_confirmed_write / after_confirmed_write
   - smoke 输出 agent_decision_after_confirmed_write_has_review_queue=True
   - 这证明“Agent 建议 -> 草稿 -> 用户确认写库 -> 下一轮上下文读回 -> 下一轮决策”的闭环成立

37. 2026-07-08 第五版 AgentRun 最小后端闭环已落地：
   - 新增 agent_runs SQLite 表，用于记录一次 Agent 运行的 context / decision 快照和反馈摘要
   - 新增 backend/app/services/agent_run_service.py
   - 新增 POST /api/agent/runs、GET /api/agent/runs、GET /api/agent/runs/{run_id}、PATCH /api/agent/runs/{run_id}
   - 列表接口只返回 contextSummary / decisionSummary / feedbackSummary 摘要，详情接口返回完整 contextSnapshot / decisionSnapshot
   - PATCH run 状态时会重新汇总同目标 ActionLog 反馈，形成运行轨迹和反馈记录的关联
   - 新增测试覆盖创建、列表、详情、状态更新、缺失资源和用户隔离
   - smoke 输出 agent_run_count、agent_run_status、agent_run_feedback_total、agent_run_latest_feedback_status

38. 2026-07-08 第五版 Tool Registry 最小功能包已落地：
   - 新增 backend/app/services/agent_tool_registry_service.py，定义 review_material、create_review_draft、create_task_draft、suggest_material_gap、answer_only 五类工具
   - 新增 GET /api/agent/tools 返回工具注册表
   - AgentDecision proposedActions 现在返回 toolName、toolLabel、riskLevel、draftOnly、applyTarget
   - 兼容现有 actionType：create_flashcards / create_quiz 映射到 create_review_draft，任务类动作映射到 create_task_draft
   - 高风险 create_task_draft 会强制 requiresConfirmation=true
   - 前端智能体工作台动作卡展示工具名、风险等级、草稿态和执行目标
   - smoke 输出 agent_tool_count、agent_decision_first_tool、agent_decision_first_risk、agent_decision_first_draft_only

39. 2026-07-08 第五版 LLM JSON Decision hybrid 最小功能包已落地：
   - 新增 backend/app/services/agent_decision_provider.py
   - POST /api/agent/decide 新增 decisionMode=rule-based / llm-json / hybrid 查询参数，默认仍为 rule-based
   - hybrid/llm-json 会尝试让 OpenAI-compatible Provider 输出结构化 AgentDecision JSON
   - JSON 解析失败、字段缺失、非法 actionType 或 mock 环境不可用时自动回退 rule-based
   - 回退结果保留 requestedMode、fallbackReason 和 reflection 字段，便于解释模型增强是否生效
   - LLM 输出的 proposedActions 会经过 Tool Registry 校验和 enrich，高风险工具仍强制 requiresConfirmation
   - 新增测试覆盖 hybrid 成功解析和非法 action 回退
   - smoke 输出 agent_hybrid_decision_mode、agent_hybrid_requested_mode、agent_hybrid_fallback

40. 2026-07-09 第五版 Decision Guard 输出评审功能包已落地：
   - 新增 backend/app/services/agent_decision_guard_service.py
   - LLM JSON Decision 输出先经过 Decision Guard，再进入 Tool Registry 风险映射
   - decisionGuard.status 支持 accepted / sanitized / fallback
   - 未知 nextAction 或 proposedActions.type 会被 Guard 拒绝并回退 rule-based
   - 高风险或 draftOnly 动作如果模型漏标 requiresConfirmation，会被 Guard 修正为必须确认
   - smoke 输出 agent_hybrid_guard_status，便于验收模型输出治理是否生效

41. 2026-07-09 Decision Guard 前端可观察化和截图证据已收口：
   - app/modules/agent-workbench.js 已展示 hybrid/rule-based 模式切换、Guard 状态、fallback 原因和回退后的动作草案
   - 浏览器验收截图已保存为 docs/images/agent-guard-hybrid-fallback.png
   - README.md 已加入 Decision Guard 可观察化截图，作为项目主页证据
   - rule-based 对比图只作为本地验收证据，不进入 README
```

## 已验证

```text
第一版 MVP 主流程已通过验收。
python -m pytest backend/tests -v 曾通过。
python backend/smoke_api.py 曾通过。
dev 前端完整演示曾通过：创建目标 -> 添加资料 -> AI 整理 -> 生成行动计划 -> 今日任务打卡 -> 查看进度 -> 闪卡/测试题 -> 成长问答。
RAG chunks 基础能力已合入 dev，并有接口说明和测试覆盖。
赵分支 chunks 前端闭环已验证，并已在 dev 复跑：
- python -m pytest backend/tests -v：10 passed
- python backend/smoke_api.py：通过，包含 chunk_count / chunk_readback / chunk_search_count
- node --check app/api.js app/app.js app/modules/materials.js：通过
- Edge 浏览器 smoke：dev 上新增资料 -> 生成片段 -> 搜索 review tasks 命中，无控制台错误
成长问答页 agent ask 前端接入已在 dev 复跑：
- python -m pytest backend/tests -v：14 passed
- python backend/smoke_api.py：通过，包含 agent_mode / agent_reference_count / agent_from_material / agent_confidence
- node --check app/api.js app/app.js app/modules/chat.js app/modules/materials.js：通过
- Edge 浏览器 smoke：dev 上新增资料 -> 生成 chunks -> 成长问答提问 -> 展示回答依据、学习建议、来源片段和 confidence，无控制台错误
资料总结区问 AI 入口已在本地 Edge headless 验证：
- 新增目标和资料 -> 生成 chunks -> 点击资料总结区“问 AI” -> 自动切到成长问答页并预填问题
- 发送后展示回答依据、学习建议、confidence 和 3 条来源片段
- 传入 materialId 后，GET /api/materials/{material_id}/qa 可读回 1 条对应资料问答记录
- 控制台无 error / pageerror
资料问答历史前端接入当前分支已完成静态检查：
- node --check app/api.js app/app.js app/modules/chat.js app/modules/materials.js：通过
- python -m pytest backend/tests -v --basetemp .pytest_tmp_v2_qa_frontend：15 passed
- python backend/smoke_api.py：通过，包含 qa_record_count: 1
- python -m compileall backend：通过
LLM Provider 抽象当前分支已完成后端验证：
- python -m pytest backend/tests -v --basetemp .pytest_tmp_llm_provider：18 passed
- python backend/smoke_api.py：通过，包含 agent_mode: mock / qa_record_count: 1
- python -m compileall backend：通过
资料历史问答复盘入口当前分支已完成验证：
- node --check app/api.js app/app.js app/modules/chat.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/utils.js：通过
- python -m pytest backend/tests -v：18 passed
- python backend/smoke_api.py：通过，包含 qa_record_count: 1
- Edge 浏览器 smoke：资料 QA 历史 -> 转闪卡草稿 -> 记忆页展示；重复点击不重复；删除新建 smoke 资料后复盘草稿清零；无 console error / pageerror
第二版公共验收文档收口当前分支已完成基线验证：
- node --check app/api.js app/app.js app/modules/chat.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/utils.js：通过
- python -m pytest backend/tests -v：18 passed
- python backend/smoke_api.py：通过，包含 qa_record_count=1
OpenAI-compatible Provider 入口当前分支已完成验证：
- python -m pytest backend/tests/test_llm_provider.py -v：6 passed
- python -m pytest backend/tests -v：21 passed
- python backend/smoke_api.py：通过，默认 agent_mode=mock 且 qa_record_count=1
- python -m compileall backend：通过
- node --check app/api.js app/app.js app/modules/chat.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/utils.js：通过
真实模型人工联调当前分支已完成验证：
- 智谱 glm-4.7-flash 返回 429：模型当前访问量过大
- 智谱 glm-4-flash-250414 三类问题直连 Provider 联调通过
- Edge / Playwright 浏览器主流程通过：资料 -> chunks -> 真实模型问答 -> 历史读回 mode=openai-compatible -> 复盘草稿，console error / pageerror 为 0
- python -m pytest backend/tests -v：23 passed
- python backend/smoke_api.py：通过，默认 agent_mode=mock 且 qa_record_count=1
- python -m compileall backend：通过
本地 .env 配置支持当前分支已完成验证：
- backend/.env.example 已新增，backend/.env 已明确加入 .gitignore
- 后端会读取 backend/.env，但不会覆盖系统环境变量
- python -m pytest backend/tests/test_llm_provider.py -v：10 passed
- python -m pytest backend/tests -v：25 passed
- python backend/smoke_api.py：通过，默认 agent_mode=mock 且 qa_record_count=1
- python -m compileall backend：通过
本地 backend/.env 真实配置已完成 API 验证：
- 8001 临时后端仅通过 backend/.env 读取智谱配置
- API smoke 返回 mode=openai-compatible、confidence=high、isFromMaterial=true
- 默认测试和 smoke 使用 LLM_ENV_FILE 指向不存在文件隔离本地 Key，仍保持 mock 基线
闪卡复习闭环当前 dev 已完成验证：
- python -m pytest backend/tests/test_material_flow.py：3 passed
- LLM_ENV_FILE 指向不存在文件时 python -m pytest：33 passed
- python -m compileall backend：通过
- node --check app/api.js app/app.js app/modules/materials.js app/modules/utils.js：通过
- 8001 接口 smoke：临时资料 -> 创建单张闪卡 -> PATCH 标记 known -> GET 读回 -> 删除临时资料，通过
2026-07-06 本地 AI 学习能力和问答入口优化已完成基础验证：
- python -m pytest backend/tests -q --basetemp .tmp_pytest_ai_summary_final -o cache_dir=.tmp_pytest_ai_summary_final_cache：33 passed
- python -m py_compile backend/app/services/material_ai_service.py backend/app/services/llm_provider.py backend/app/services/ai_learning_service.py：通过
- node --check app/modules/materials.js：通过
- node --check app/app.js：通过
- 智谱最小连接测试：真实网络权限下 status 200，model=glm-4-flash
- 资料总结接口验证：aiMode=openai-compatible，difficulties 已返回分析型难点
- 测试题批改接口验证：mode=openai-compatible 可返回 score / feedback / suggestion
第三版资料学习智能体闭环已在 dev 完成合并验证：
- git fetch origin dev 后同步到 6287b6c，并在 feature/v3-agent-action-entry 合并最新 dev
- 合并后无冲突标记；app/index.html 和 app/styles.css 自动合并成功
- node --check app/app.js app/modules/chat.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/api.js app/modules/utils.js：通过
- python -m compileall backend/app：通过
- LLM_PROVIDER=mock 时 python -m pytest：38 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_next_action=review_material、agent_requires_confirmation=True、qa_record_count=1
- 第一次未强制 mock 的 pytest 受本地真实模型环境影响，部分用例返回 openai-compatible；这不是合并冲突，mock 回归已通过
- 已推送 origin/dev 到 9abad5d
第四版 AgentContext 只读接口当前分支已完成基础验证：
- LLM_PROVIDER=mock 时 python -m pytest backend/tests/test_agent_flow.py -q --basetemp .tmp_pytest_agent_context -o cache_dir=.tmp_pytest_agent_context_cache：9 passed
- backend/smoke_api.py 已加入 agent_context_goal_count、agent_context_material_count、agent_context_task_total、agent_context_flashcard_total 输出
第四版智能体工作台当前分支已完成基础验证：
- node --check app/api.js app/app.js app/modules/agent-workbench.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/chat.js app/modules/utils.js：通过
- LLM_PROVIDER=mock 时 python -m pytest backend/tests -q --basetemp .tmp_pytest_agent_workbench -o cache_dir=.tmp_pytest_agent_workbench_cache：40 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_context_* 输出
- 临时 8011 后端 HTTP 验证 GET /api/agent/context：200
- 当前 8001 如果仍是旧 uvicorn 进程，需要重启后端后才能访问 /api/agent/context
第四版 AgentDecision 当前分支已完成基础验证：
- LLM_PROVIDER=mock 时 python -m pytest backend/tests/test_agent_flow.py -q --basetemp .tmp_pytest_agent_decision -o cache_dir=.tmp_pytest_agent_decision_cache：11 passed
- node --check app/api.js app/app.js app/modules/agent-workbench.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/chat.js app/modules/utils.js：通过
- LLM_PROVIDER=mock 时 python -m pytest backend/tests -q --basetemp .tmp_pytest_agent_decision_all -o cache_dir=.tmp_pytest_agent_decision_all_cache：42 passed
- python -m compileall backend/app：通过
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_decision_mode=rule-based、agent_decision_next_action、agent_decision_problem_count、agent_decision_action_count
- 临时 8012 后端 HTTP 验证 POST /api/agent/decide：200
第四版 AgentActionLog 当前分支已完成基础验证：
- LLM_PROVIDER=mock 时 python -m pytest backend/tests/test_agent_flow.py -q --basetemp .tmp_pytest_agent_action_log_final -o cache_dir=.tmp_pytest_agent_action_log_final_cache：13 passed
- node --check app/api.js app/app.js app/modules/agent-workbench.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/chat.js app/modules/utils.js：通过
- python -m compileall backend/app：通过
- LLM_PROVIDER=mock 时 python -m pytest backend/tests -q --basetemp .tmp_pytest_agent_action_log_all -o cache_dir=.tmp_pytest_agent_action_log_all_cache：44 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_action_log_count=1、agent_action_log_status=later
- 临时 8013 后端 HTTP 验证 POST /api/agent/action-logs 和 GET /api/agent/action-logs：200
第四版 ActionLog applied 状态闭环当前分支已完成基础验证：
- node --check app/api.js app/app.js app/modules/agent-workbench.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/chat.js app/modules/utils.js：通过
- LLM_PROVIDER=mock 时 python -m pytest backend/tests/test_agent_flow.py -q --basetemp .tmp_pytest_agent_applied -o cache_dir=.tmp_pytest_agent_applied_cache：13 passed
- python -m compileall backend/app：通过
- LLM_PROVIDER=mock 时 python -m pytest backend/tests -q --basetemp .tmp_pytest_agent_applied_all -o cache_dir=.tmp_pytest_agent_applied_all_cache：44 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_action_log_applied_status=applied
第四版低风险 proposedAction 执行入口当前分支已完成基础验证：
- node --check app/api.js app/app.js app/modules/agent-workbench.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/chat.js app/modules/utils.js：通过
- LLM_PROVIDER=mock 时 python -m pytest backend/tests -q --basetemp .tmp_pytest_agent_execute_route -o cache_dir=.tmp_pytest_agent_execute_route_cache：44 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_action_log_applied_status=applied
第四版草稿态执行当前分支已完成基础静态验证：
- node --check app/api.js app/app.js app/modules/agent-workbench.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/chat.js app/modules/utils.js：通过
第四版任务草稿态执行当前分支已完成基础验证：
- node --check app/api.js app/app.js app/modules/agent-workbench.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/chat.js app/modules/utils.js：通过
- LLM_PROVIDER=mock 时 python -m pytest backend/tests -q --basetemp .tmp_pytest_agent_task_draft -o cache_dir=.tmp_pytest_agent_task_draft_cache：44 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_action_log_applied_status=applied
第四版上下文回读验收当前分支已完成验证：
- node --check app/api.js app/app.js app/modules/agent-workbench.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/chat.js app/modules/utils.js：通过
- LLM_PROVIDER=mock 时 python -m pytest backend/tests/test_agent_flow.py -q --basetemp .tmp_pytest_agent_readback_one -o cache_dir=.tmp_pytest_agent_readback_one_cache：14 passed
- LLM_PROVIDER=mock 时 python -m pytest backend/tests -q --basetemp .tmp_pytest_agent_readback_all -o cache_dir=.tmp_pytest_agent_readback_all_cache：45 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_context_flashcard_before_confirmed_write=1、agent_context_flashcard_after_confirmed_write=2、agent_decision_after_confirmed_write_has_review_queue=True
第五版 AgentRun 最小后端闭环当前分支已完成验证：
- LLM_PROVIDER=mock 时 pytest backend/tests --basetemp .test_artifacts\pytest-agent-run-full：49 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_run_count=1、agent_run_status=feedback_recorded、agent_run_feedback_total=1、agent_run_latest_feedback_status=applied
- python -m py_compile backend/app/services/agent_run_service.py backend/app/routers/agent.py backend/app/schemas/agent.py backend/app/services/store.py：通过
第五版 Tool Registry 最小功能包当前分支已完成验证：
- LLM_PROVIDER=mock 时 pytest backend/tests --basetemp .test_artifacts\pytest-tool-registry-full：50 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_tool_count=5、agent_decision_first_tool=review_material、agent_decision_first_risk=low
- node --check app/api.js app/modules/agent-workbench.js：通过
- python -m py_compile backend/app/services/agent_tool_registry_service.py backend/app/services/agent_decision_service.py backend/app/routers/agent.py：通过
第五版 LLM JSON Decision hybrid 最小功能包当前分支已完成验证：
- LLM_PROVIDER=mock 时 pytest backend/tests --basetemp .test_artifacts\pytest-llm-decision-full：52 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_hybrid_decision_mode=rule-based、agent_hybrid_requested_mode=hybrid、agent_hybrid_fallback=True
- node --check app/api.js app/modules/agent-workbench.js：通过
- python -m py_compile backend/app/services/agent_decision_provider.py backend/app/services/agent_decision_service.py backend/app/routers/agent.py：通过
第五版 Decision Guard 输出评审功能包当前分支已完成验证：
- LLM_PROVIDER=mock 时 pytest backend/tests/test_agent_flow.py --basetemp .test_artifacts\pytest-guard-agent-flow：22 passed
- LLM_PROVIDER=mock 时 pytest backend/tests --basetemp .test_artifacts\pytest-guard-all：53 passed
- LLM_PROVIDER=mock 时 python backend/smoke_api.py：通过，包含 agent_hybrid_guard_status=fallback
- node --check app/api.js app/modules/agent-workbench.js：通过
- python -m py_compile backend/app/services/agent_decision_guard_service.py backend/app/services/agent_decision_provider.py：通过
Decision Guard 前端可观察化截图验收已完成：
- hybrid 截图显示 hybrid -> rule-based、Decision Guard 已回退、fallbackReason 和回退后的动作草案
- rule-based 对比截图显示正常建议卡片且无 Guard 面板
- README 使用 docs/images/agent-guard-hybrid-fallback.png 作为长期文档资产
```

下一次开始开发前建议重新执行：

```text
python -m pytest backend/tests -v
python backend/smoke_api.py
```

## 当前未解决问题

```text
1. README、最终指标唯一来源、简历材料、面试讲解、演示视频对应检查和最终验收报告尚未统一冻结。
```

## 当前边界

```text
第七版已经完成 PDF/Markdown/TXT 摄取、Render/Neon 部署、PostgreSQL/pgvector 混合检索、带引用问答、固定评测、CI 和作品集交付。
本轮只做 UX-04、UX-02、第三周总门禁和 CLOSE-02 -> CLOSE-01 -> CLOSE-03 -> CLOSE-04，不扩张为通用 AutoGPT 或多 Agent 平台。
不实施 MCP、多模型比较、通知、周报、联网研究、OCR、PDF 查看器、长期记忆、个性化 FSRS、行为埋点、UI 框架迁移、更多页面或产品改名。
草稿工具仍不能绕过确认直接写入正式 tasks / flashcards；真实模型失败样例与固定评测口径继续保留。
```

## 最新入口文档

```text
docs/README.md
docs/planning/个人学习助手后续任务执行文档.md
docs/planning/第七版作品集化与公开演示需求文档.md
docs/planning/AI-Agent简历与面试讲解稿.md
docs/portfolio/架构与评测边界.md
docs/portfolio/简历材料.md
docs/evaluation/第七版Batch4Agent真实模型评测报告.md
```

## 下一步第一件事

```text
执行 CLOSE-04 求职交付冻结：
1. 汇总现有固定评测、REL/LOOP/CLOSE 门禁和约束，建立最终指标唯一来源。
2. 将 README、`docs/portfolio/简历材料.md`、`docs/planning/AI-Agent简历与面试讲解稿.md` 和最终验收报告统一为同一项目定位、指标和限制。
3. 对照既有 91 秒视频的路径与当前黄金流程；仅记录对应关系和限制，不因补充内容重录。
4. 执行链接、数字、敏感内容和 diff 检查后独立提交；不进行任何远程操作。
```

