# FE-01 设计令牌与组件整理验收记录

验收日期：2026-07-24

分支：`feature/learning-loop-ux`

基线：`69b5729`

实现提交：`d4aa0de` (`refactor(frontend): consolidate design tokens and components`)

状态：**通过**

## 修改范围

- 在 `app/styles.css` 建立语义颜色、表面、文本、边框、状态、焦点、圆角、间距和层级令牌，并按基础元素、门户、壳层、通用组件、响应式和减少动效分区。
- 合并按钮、卡片、状态徽标、空状态和加载表面的共享样式；主交互元素具备一致的焦点和禁用反馈。
- 在 `tests/browser/v7-frontend-refresh.spec.cjs` 增加 `FE-01` 令牌、焦点、按钮样式、移动端溢出和 axe 断言。
- 保存并人工复核桌面门户与移动 Today 证据图；同轮完整 E2E 生成的 8 张黄金流程截图已保留。

## 自动化验证

| 命令 | 结果 |
|---|---|
| `npx.cmd playwright test --config=tests/browser/playwright.v7-batch4.config.cjs --grep "FE-01"` | `1 passed` |
| `npm.cmd run check` | 通过 |
| `npm.cmd run test:e2e` | `12 passed (2.1m)` |
| `python backend/check_sensitive_content.py` | 通过 |
| `git diff --check` | 通过，仅有 CRLF 换行提示 |

## 人工截图复核

- `docs/images/fe01-auth-portal-desktop.png`：登录门户在 `1440×900` 中层级清楚，表单、主入口和深浅表面对比可读。
- `docs/images/fe01-today-mobile.png`：Today 在 `390×844` 中显示首屏目标、指标与底部导航，无横向溢出或遮挡。
- `close02`、`close03`、`ux02`、`ux03`、`ux04` 同轮重写图：均已打开确认，未见错误 Toast、加载残影、敏感数据或不可达主要操作。

## 已知例外

登录门户的渐变、阴影、深色图层及少量状态色继续使用硬编码值。这些值用于特定视觉层或明确状态，未与通用组件语义冲突；页面级信息层级工作由后续阶段处理。

## 下一阶段

仅启动 `FE-02`：登录、注册、Demo 和错误反馈精修；不修改认证 API、Cookie、限流或密码实现。
