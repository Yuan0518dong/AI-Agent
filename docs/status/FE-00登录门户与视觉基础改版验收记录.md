# FE-00 登录门户与视觉基础改版验收记录

验收日期：2026-07-24

分支：`feature/learning-loop-ux`

基线：`69b5729`

状态：**通过**

## 目标

收口当前工作区已实现的登录门户与视觉基础改版，消除测试不稳定与未经复核的截图变更，形成可回滚的独立提交。

## 修改范围

| 区域 | 文件 |
|---|---|
| 前端展示层 | `app/index.html`、`app/styles.css`、`app/app.js` |
| 浏览器测试 | `tests/browser/v7-frontend-refresh.spec.cjs`（新增） |
| 选择器/竞态修复 | `tests/browser/v7-batch2.spec.cjs`、`v7-close02.spec.cjs`、`v7-close03.spec.cjs`、`playwright.v7-batch4.config.cjs` |
| 计划与验收 | `docs/planning/前端与登录门户完整优化执行文档.md`、本记录 |
| 证据截图 | `docs/images/` 下本轮 E2E 重新生成的已跟踪 PNG |

## 用户可见结果

- 登录页移除外部 Unsplash 依赖，改为代码原生的 Agent/RAG 学习闭环展示。
- 登录门户、全局颜色、侧栏、卡片、按钮、输入框与移动底部导航完成第一轮视觉改版。
- 登录/注册标签具备语义、方向键切换与动态标题。
- 主色 mint 调整为 `#0f766e`，消除 `v7-ux04` 文本对比度失败。

## 自动化验证

| 命令/范围 | 结果 |
|---|---|
| `npx.cmd playwright test tests/browser/v7-frontend-refresh.spec.cjs --config=tests/browser/playwright.v7-batch4.config.cjs` | 通过 |
| `npx.cmd playwright test tests/browser/v7-ux04.spec.cjs --config=tests/browser/playwright.v7-batch4.config.cjs` | 通过（mint 对比度 `#0f766e`） |
| `npx.cmd playwright test tests/browser/v7-close03.spec.cjs --config=tests/browser/playwright.v7-batch4.config.cjs` | 3 passed（闪卡评分 `expect.poll()` 竞态修复后） |
| `npx.cmd playwright test --config=tests/browser/playwright.v7-batch4.config.cjs` | **11 passed (~1.9m)** |
| `npm.cmd run check` | 通过 |
| `python backend/check_sensitive_content.py` | 通过 |
| `git diff --check` | 仅 CRLF 换行提示，无空白错误 |
| axe serious/critical | 0（由相关 Playwright 断言覆盖） |

### 闪卡评分失败根因

- 现象：点击 `#card-known` 后测试立即读取 API，偶发读到旧状态。
- 判定：异步竞态，非产品逻辑错误；页面最终状态正确。
- 修复：仅将即时读取改为 `expect.poll()`，完整保留业务断言：
  `{ reviewCount: 1, lastRating: "good", status: "known" }`。
- 未删除断言、未跳过测试、未放宽 axe 级别、未改后端评分逻辑。

## 人工验证

维护者已逐张打开本轮证据截图并完成复核（2026-07-24），检查项：

- 无横向溢出；主要内容未被侧栏或底部导航遮挡。
- 无错误 Toast、无加载残影。
- 无真实账号、Cookie、Key、连接串或调试面板。
- 页面名称、当前任务与主要操作可读。

### 截图清单

| 截图 | 复核 |
|---|---|
| `docs/images/close02-golden-flow-mobile.png` | 通过 |
| `docs/images/close03-first-use-role.png` | 通过 |
| `docs/images/close03-high-risk-role.png` | 通过 |
| `docs/images/close03-weak-answer-role.png` | 通过（同轮 E2E 同步更新） |
| `docs/images/ux01-goal-shortcuts-mobile.png` | 通过 |
| `docs/images/ux02-desktop-navigation.png` | 通过 |
| `docs/images/ux02-mobile-navigation.png` | 通过 |
| `docs/images/ux03-today-actions-mobile.png` | 通过 |
| `docs/images/ux04-agent-current-action-desktop.png` | 通过 |
| `docs/images/ux04-agent-current-action-mobile.png` | 通过（同轮 E2E 同步更新） |
| `docs/images/v7-batch4-mobile-e2e.png` | 通过 |

## 形成的求职证据

- 可演示的原生 HTML/CSS/JS 登录门户与产品壳层，不依赖外部营销图。
- 可回归的 Playwright 前端刷新与黄金闭环用例，含 a11y 与响应式约束。
- 测试不稳定采用轮询修复而非降低断言，体现工程纪律。

## 已知限制

- 本阶段仅收口第一轮视觉基础；设计令牌整理、页面层级优化分别在 `FE-01` 及后续阶段。
- 未引入前端框架或 CSS 构建工具；后端 API、数据库与业务模块保持冻结。
- 本提交不含录屏/作品集材料更新（属 `FE-07`）。

## 下一阶段第一件事

按计划启动 `FE-01`：整理 `app/styles.css` 设计令牌与组件分区，并在 `v7-frontend-refresh.spec.cjs` 增加 `FE-01` 断言。
