# 真实模型联调记录

更新时间：2026-07-03

## 1. 当前状态

```text
当前分支：feature/real-llm-smoke
目标：用真实 API Key 验证 OpenAI-compatible Provider 是否能跑通第二版资料问答闭环。
当前代码状态：OpenAI-compatible Provider 已接入；未配置密钥时默认回退 MockLLMProvider。
```

## 2. 联调配置

实际联调时填写：

```text
服务商：
模型：
LLM_PROVIDER=openai-compatible
LLM_BASE_URL：
LLM_MODEL：
是否使用真实 API Key：否 / 是
```

不要把 `LLM_API_KEY` 写入本文档或提交到 Git。

## 3. 启动命令

PowerShell 示例：

```powershell
$env:LLM_PROVIDER="openai-compatible"
$env:LLM_API_KEY="你的密钥"
$env:LLM_MODEL="模型名"
$env:LLM_BASE_URL="服务商 base url"
$env:LLM_TIMEOUT_SECONDS="20"
python -m uvicorn backend.app.main:app --reload
```

前端：

```bash
python -m http.server 5500 --directory app
```

访问：

```text
http://127.0.0.1:5500/index.html
```

## 4. 三类验收问题

### 4.1 资料中有明确答案

```text
测试资料：
RAG uses retrieval to find relevant chunks before generating an answer.
Retrieval grounds the answer in material evidence and reduces unsupported responses.

测试问题：
RAG 如何把回答建立在资料证据上？

结果：
是否通过：
问题记录：
```

### 4.2 资料中只有部分答案

```text
测试资料：
Spaced repetition helps learners review difficult knowledge over time.
Flashcards can be used to check whether a learner can recall key ideas.

测试问题：
这份资料是否说明了如何用 LangChain 实现间隔重复？

结果：
是否通过：
问题记录：
```

### 4.3 资料中没有答案

```text
测试资料：
This note describes how to create flashcards from learning materials.
It does not discuss deployment, authentication, or payment systems.

测试问题：
这个系统应该如何设计登录和支付？

结果：
是否通过：
问题记录：
```

## 5. 联调结论

```text
真实 Provider 是否可用：
资料不足提示是否清楚：
是否出现编造：
是否建议继续收紧 prompt：
是否建议第二版合 main：
```
