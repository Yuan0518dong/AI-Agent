# 第二版最终验收记录

更新时间：2026-07-03

## 1. 验收结论

```text
第二版 dev 阶段验收通过。
当前 dev 已具备可演示、可回归、可推送 main 的第二版候选状态。
```

本轮第二版收口版本：

```text
分支：dev
远程：origin/dev
功能闭环提交：9d7591f merge: complete flashcard review loop
收口文档提交：docs: record v2 final acceptance
```

## 2. 已完成主闭环

```text
资料录入
-> 资料摘要和练习生成
-> chunks 生成和关键词检索
-> 基于资料提问
-> 展示 answer / basis / suggestion / references / confidence
-> 保存并读回历史问答
-> 从历史问答生成复盘草稿
-> 用户确认加入正式闪卡
-> 闪卡 known / review 状态保存
```

## 3. 最终验收结果

```text
python -m pytest backend/tests -v：26 passed
python backend/smoke_api.py：通过
node --check app/api.js app/app.js app/modules/chat.js app/modules/materials.js app/modules/goals.js app/modules/progress.js app/modules/utils.js：通过
python -m compileall backend/app：通过
```

API smoke 关键结果：

```text
agent_mode=mock
agent_reference_count=1
agent_from_material=True
agent_confidence=medium
qa_record_count=1
flashcard_count=1
flashcard_readback=True
quiz_readback=True
checkin_done=True
completion_rate=33
material_deleted=True
```

## 4. 真实模型联调结论

```text
OpenAI-compatible Provider 已完成入口和本地 .env 配置支持。
智谱 glm-4-flash-250414 已完成三类问题人工联调。
默认测试和 smoke 使用 mock 基线，不依赖真实 API Key。
真实 API Key 只保存在本地 backend/.env，不写入 Git。
```

## 5. 当前边界

```text
1. 第二版不做登录、用户隔离、PDF 上传、正式部署。
2. 第二版不做向量数据库和完整 RAG。
3. 第二版不做完整 Agent 循环。
4. 第二版不做完整间隔重复算法，只保存 new / known / review 状态。
5. LangChain 问答链暂不进入本次收口，后续作为第三阶段或第二版后续增强。
```

## 6. main 合并建议

```text
建议可以进入 dev -> main 合并判断。
合 main 前建议再做一次人工浏览器演示确认：
前端 http://127.0.0.1:5500/index.html
后端 http://127.0.0.1:8001 或重新启动到 README 默认 8000
```

如果人工演示无异常，可以执行：

```bash
git switch main
git merge --no-ff dev -m "merge: release v2 learning workflow"
git push origin main
```
