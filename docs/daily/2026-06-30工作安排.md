# 2026-06-30 工作安排

日期：2026-06-30

参与成员：陈、赵

## 1. 今日目标

今天的目标不是继续增加零散页面，而是围绕第一阶段 MVP，把两个人的开发任务拆清楚，并各自推进一个可以落地的业务模块。

第一阶段主线：

```text
目标 -> 资料 -> AI 整理 -> 计划 -> 打卡 -> 进度
```

今天重点先完成：

```text
1. 明确陈、赵各自负责的模块边界。
2. 各自开始修改当前静态原型。
3. 补充对应数据对象和 API 草案。
4. 为后续 MVP-1 工程化版本做准备。
```

暂不作为今天硬性任务：

```text
注册登录
PDF 上传
真实大模型接入
正式后端开发
正式部署
```

## 2. 今日分工原则

今天不按“一个人前端、一个人后端”来分，而是按业务模块分工。

每个人负责自己模块的：

```text
页面交互
数据对象
业务逻辑
localStorage 静态原型实现
API 草案
验收说明
```

这样每个人都能参与具体实现细节，而不是只做文档、测试或演示。

## 3. 陈今日任务

陈负责模块：

```text
目标管理 -> 行动计划 -> 今日任务 -> 打卡 -> 进度统计
```

### 3.1 任务一：梳理并补充数据对象

需要整理以下数据对象字段：

```text
Goal
PlanTask
CheckInRecord
Progress
```

建议字段：

```js
Goal = {
  id,
  name,
  subject,
  level,
  deadline,
  dailyMinutes,
  notes,
  createdAt,
  updatedAt
}
```

```js
PlanTask = {
  id,
  goalId,
  title,
  detail,
  date,
  priority,
  done,
  completedAt
}
```

```js
CheckInRecord = {
  id,
  goalId,
  taskId,
  date,
  status,
  checkedAt
}
```

```js
Progress = {
  goalId,
  totalTasks,
  completedTasks,
  completionRate,
  todayTotal,
  todayCompleted
}
```

说明：

```text
Progress 可以先不单独保存，通过 tasks 计算得到。
```

### 3.2 任务二：修改目标管理静态原型

当前原型已有：

```text
创建目标
目标列表
删除目标
```

今天建议补：

```text
编辑目标
目标更新时间 updatedAt
保存编辑后重新渲染目标列表
```

实现位置：

```text
app/index.html
app/app.js
app/styles.css
```

最低完成标准：

```text
用户可以点击目标上的“编辑”按钮。
表单能回填该目标已有内容。
用户修改后可以保存。
保存后目标列表显示最新内容。
刷新页面后数据不丢失。
```

### 3.3 任务三：修改今日任务逻辑

当前问题：

```text
今日任务页面现在显示的是任务列表前 10 条，不是真正的今日任务。
```

今天要改为：

```text
只显示 date 等于今天的任务。
如果今天没有任务，显示空状态。
```

最低完成标准：

```text
任务生成后，每个任务都有 date 字段。
今日页面只展示当天任务。
非今日任务不出现在今日任务区域。
```

### 3.4 任务四：优化打卡状态

当前逻辑：

```text
勾选 checkbox 后，只修改 task.done。
```

今天建议改为：

```text
勾选任务时：
done = true
completedAt = 当前时间

取消勾选时：
done = false
completedAt = null
```

最低完成标准：

```text
任务完成状态可以保存。
取消完成后状态可以恢复。
刷新页面后完成状态仍然正确。
```

### 3.5 任务五：优化进度统计

当前已有：

```text
每个目标的任务完成率。
```

今天建议补充：

```text
总任务数
已完成任务数
今日任务数
今日已完成数
完成率
```

最低完成标准：

```text
进度页能看到每个目标的完成率。
至少能展示总任务数和已完成任务数。
首页指标能随着打卡状态变化。
```

### 3.6 陈今日交付物

```text
1. app 中目标、计划、打卡、进度相关静态原型改动。
2. Goal / PlanTask / CheckInRecord / Progress 字段说明。
3. 目标、任务、打卡、进度 API 草案。
4. 简短说明：今天完成了什么、还有什么没完成。
```

## 4. 赵今日任务

赵负责模块：

```text
资料管理 -> AI 整理 -> 闪卡 / 测试题 -> 成长问答
```

### 4.1 任务一：梳理并补充数据对象

需要整理以下数据对象字段：

```text
Material
MaterialSummary
Flashcard
QuizQuestion
AIConversation
```

建议字段：

```js
Material = {
  id,
  title,
  type,
  content,
  createdAt,
  updatedAt
}
```

```js
MaterialSummary = {
  materialId,
  overview,
  keyPoints,
  difficulties,
  studyOrder,
  actionItems,
  createdAt
}
```

```js
Flashcard = {
  id,
  materialId,
  front,
  back,
  status,
  createdAt
}
```

```js
QuizQuestion = {
  id,
  materialId,
  question,
  answer,
  type,
  createdAt
}
```

```js
AIConversation = {
  id,
  messages,
  relatedMaterialIds,
  createdAt
}
```

### 4.2 任务二：优化资料管理静态原型

当前原型已有：

```text
添加资料
资料列表
删除资料
自动生成简单摘要
```

今天建议补：

```text
资料详情展示
资料更新时间 updatedAt
资料删除时同步删除对应闪卡和测试题
```

最低完成标准：

```text
用户能看到资料标题、类型、摘要和知识点。
用户能查看资料详情或完整整理结果。
删除资料后，相关闪卡和测试题不会残留。
刷新页面后资料数据不丢失。
```

### 4.3 任务三：结构化 AI 整理结果

当前 summary 主要包括：

```text
overview
keyPoints
```

今天建议扩展为：

```text
overview
keyPoints
difficulties
studyOrder
actionItems
```

最低完成标准：

```text
添加资料后能生成结构化整理结果。
资料总结页能展示摘要、知识点、难点或学习顺序。
即使是 mock AI，也要保证输出结构接近真实 AI 接口。
```

### 4.4 任务四：优化闪卡和测试题关联

当前问题：

```text
闪卡通过 source: material.title 关联资料，不够稳定。
```

今天建议改为：

```text
使用 materialId 关联资料。
```

最低完成标准：

```text
每张闪卡都有 materialId。
每道测试题都有 materialId。
删除资料时，可以通过 materialId 删除对应闪卡和测试题。
```

### 4.5 任务五：补充成长问答草案

今天不要求接真实 AI，但要补：

```text
问答消息结构
问答基于哪些资料
后续真实 AI prompt 输入输出草案
```

最低完成标准：

```text
问答记录能保存。
问答回答尽量基于已有资料摘要或知识点。
文档中写清楚真实 AI 接入时需要哪些输入。
```

### 4.6 赵今日交付物

```text
1. app 中资料、AI 整理、闪卡、测试题、问答相关静态原型改动。
2. Material / MaterialSummary / Flashcard / QuizQuestion / AIConversation 字段说明。
3. 资料、AI 整理、闪卡、问答 API 草案。
4. 简短说明：今天完成了什么、还有什么没完成。
```

## 5. 共同任务

今天两人需要共同确认：

```text
1. 第一阶段是否只做静态原型增强，不接正式后端。
2. 数据对象字段命名是否统一。
3. API 返回格式是否统一。
4. 哪些功能今天真实实现，哪些继续 mock。
5. 今天结束时是否能跑通完整演示流程。
```

建议统一 API 返回格式：

```js
{
  code: 0,
  message: "success",
  data: {}
}
```

建议统一时间字段：

```text
createdAt
updatedAt
completedAt
checkedAt
```

建议统一 ID 命名：

```text
id
goalId
materialId
taskId
```

## 6. 今日整体验收标准

今天结束时，至少要能完成以下流程：

```text
1. 创建一个成长目标。
2. 编辑这个成长目标。
3. 根据目标生成行动计划。
4. 今日页面只显示今天的任务。
5. 用户可以勾选任务完成打卡。
6. 进度页面能看到任务完成率变化。
7. 添加一份资料。
8. 系统生成结构化资料摘要和知识点。
9. 系统生成闪卡或测试题。
10. 用户可以进行一次成长问答。
11. 刷新页面后数据仍然存在。
```

如果全部完成，说明今天的静态原型已经从“可演示页面”往“可工程化业务原型”推进了一步。

## 7. 陈模块验收标准

陈负责模块通过验收，需要满足：

```text
1. 目标可以创建、编辑、删除。
2. 目标编辑后数据能保存到 localStorage。
3. 行动计划能根据目标生成任务。
4. 今日任务只展示当天任务。
5. 打卡能记录完成状态和完成时间。
6. 取消打卡后状态能恢复。
7. 进度统计能根据任务完成状态实时变化。
8. 页面刷新后目标、任务和打卡状态不丢失。
```

## 8. 赵模块验收标准

赵负责模块通过验收，需要满足：

```text
1. 资料可以添加、查看、删除。
2. 资料整理结果结构化展示。
3. 资料整理结果至少包括摘要和知识点。
4. 闪卡和测试题通过 materialId 关联资料。
5. 删除资料时，对应闪卡和测试题同步删除。
6. 成长问答能基于已有资料给出 mock 回答。
7. 页面刷新后资料、摘要、闪卡、测试题和问答记录不丢失。
```

## 9. 今日建议时间安排

```text
09:00 - 09:30 共同确认今日分工和验收标准
09:30 - 10:30 各自整理数据对象和 API 草案
10:30 - 12:00 各自修改静态原型第一轮
14:00 - 16:00 各自完成核心功能实现
16:00 - 17:00 两人交叉评审和修问题
17:00 - 17:30 跑完整演示流程
17:30 - 18:00 记录今日完成情况和明日待办
```

## 10. 明日可能衔接任务

如果今天完成顺利，明天可以进入：

```text
1. 补 docs/planning/第一版技术实现方案.md。
2. 确认 MVP-1 技术栈。
3. 建立前后端工程目录。
4. 把今天定好的数据对象转成数据库表草案。
5. 把今天定好的 API 草案转成后端接口。
```

