# 2026-07-01 工作安排

分支：`dev`

参与成员：陈、赵

## 当前判断

今天仍以 `dev` 跑通演示为第一优先级，但跑通演示由陈先独立完成，不等待赵一起联调。

当前状态：

```text
1. dev 已合入陈、赵两边成果。
2. 陈模块后端已覆盖目标、计划、今日任务、打卡、进度。
3. 赵模块仍主要在前端 localStorage/mock 层，需要开始资料模块后端迁移。
4. 陈今天先负责把 dev 完整演示跑通，并记录 P0 / P1。
5. 赵今天并行做资料模块后端第一步，先做 Material API。
6. 今天不要求前后端全部 API 化，也不建议直接合 main。
```

已验证：

```text
python backend/smoke_api.py 可通过。

health_code: 0
goal_code: 0
plan_count: 3
today_status: 200
checkin_done: true
completion_rate: 33

2026-07-01 上午补充验证：
dev 前端完整演示流程已通过。
演示路径：创建目标 -> 添加资料 -> AI 整理 -> 生成行动计划 -> 今日任务打卡 -> 查看进度 -> 闪卡/测试题 -> 成长问答。
前端控制台无报错。
P0：暂无。
演示截图：docs/status/dev-demo-smoke.png
```

## 今日联调记录

```text
联调结论：dev 主流程可演示，暂无 P0。

已跑通：
1. 打开 app/index.html。
2. 清空本地数据。
3. 创建成长目标。
4. 自动生成 7 天行动计划。
5. 今日任务打卡后完成率更新。
6. 添加资料后自动生成结构化整理结果。
7. 成长问答可基于资料给出 mock 回答并显示参考资料。
8. 闪卡和测试题自动生成。
9. 进度页可展示目标进度。
10. localStorage 可保存目标、资料、闪卡和测试题。

P0：
暂无。

P1：
1. 前端目标、任务、打卡、进度尚未正式接后端 API，当前演示仍以 localStorage 为主。
2. 合 main 前需要确认是否接受当前 localStorage/mock 版本作为稳定演示版本。

P2：
1. 资料、AI 整理、闪卡、测试题、成长问答仍主要在前端 localStorage/mock。
2. 真实 AI、PDF 上传、注册登录暂不进入今天范围。

下一步：
无 P0 后，开始目标/任务/打卡/进度前端接后端 API 的最小闭环。
```

## 赵模块合并记录

```text
feature/material-ai-memory 已按最新 dev 的内存版后端架构接入资料模块 API。

已接入：
1. GET /api/materials
2. POST /api/materials
3. GET /api/materials/{id}
4. PUT /api/materials/{id}
5. DELETE /api/materials/{id}
6. POST /api/materials/{id}/summarize
7. GET /api/materials/{id}/summary
8. GET /api/materials/{id}/flashcards
9. POST /api/materials/{id}/flashcards
10. GET /api/materials/{id}/quiz
11. POST /api/materials/{id}/quiz

当前说明：
1. 最新 dev 当前采用内存版 backend store。
2. 资料、summary、flashcards、quiz 已在内存 store 中按 materialId 关联。
3. SQLite 持久化不在本次 dev 合并中强行恢复，后续统一迁移。
4. python backend/smoke_api.py 已覆盖资料 CRUD、summary、flashcards、quiz。
```

## 7 月 1 日最终收尾记录

补写时间：2026-07-02

```text
7 月 1 日晚间到 7 月 2 日同步后，dev 已继续推进到 SQLite 持久化版本。

已确认最新 dev 包含：
1. 目标模块 SQLite 持久化：
   - goals
   - tasks
   - checkins
   - progress

2. 资料模块 SQLite 持久化：
   - materials
   - material_summaries
   - flashcards
   - quiz_questions

3. 自动化测试：
   - backend/tests/test_goal_flow.py
   - backend/tests/test_material_flow.py

4. 资料模块测试隔离机制：
   - 避免测试污染正式本地数据库。

5. 后续计划文档：
   - docs/status/MVP第一版进度评估与下一步计划.md
   - docs/daily/2026-07-02工作安排.md
```

最终同步后的关键提交：

```text
876c62a docs: 同步当前状态和项目进度记录
9c1f1e8 docs: 添加MVP进度评估和7月2日安排
085ed87 Merge pull request #11 from chenwenv/feature/material-sqlite-hardening
417a180 feat: persist material module with SQLite
3e18bce Merge pull request #9 from chenwenv/feature/goal-module-sqlite
227b2c2 feat: integrate material APIs into dev
```

最终判断：

```text
1. 7 月 1 日赵模块后端 API 任务已完成，并已在后续 dev 中升级为 SQLite 持久化版本。
2. feature/material-ai-memory 已不是最新开发基线，后续赵的开发应以最新 dev 为基线重新同步分支。
3. 7 月 2 日重点不再是补后端接口，而是资料模块前端接后端 API、刷新/重启后数据仍可读取。
4. 暂不合 main，先完成 MVP 第一版稳定闭环验收。
```

## 今日总目标

陈先在 `dev` 上跑通完整演示；如果没有 P0，再开始陈模块前端接后端 API 的第一步。赵并行推进资料模块后端第一步。

演示主线：

```text
创建目标 -> 添加资料 -> AI 整理 -> 生成行动计划 -> 今日任务打卡 -> 查看进度 -> 闪卡/测试题 -> 成长问答
```

## 陈今日任务

主责：

```text
dev 演示跑通 -> 目标/计划/今日任务/打卡/进度前端接 API
```

任务：

```text
1. 拉取并确认最新 dev。
2. 执行 python backend/smoke_api.py，确认后端 API 冒烟通过。
3. 打开 app/index.html，独立跑完整 dev 演示：
   - 创建目标
   - 添加资料
   - AI 整理
   - 生成行动计划
   - 今日任务打卡
   - 查看进度
   - 闪卡/测试题
   - 成长问答
4. 记录演示中的 P0 / P1 问题。
5. 如果发现 P0，下午只修 P0，不做新 API 接入。
6. 如果没有 P0，开始陈模块前端接 API 的最小闭环：
   - 新增 API_BASE
   - 创建目标接 POST /api/goals
   - 初始化目标接 GET /api/goals
   - 生成计划接 POST /api/goals/{goal_id}/plans
   - 今日任务接 GET /api/tasks/today
   - 打卡接 POST /api/tasks/{task_id}/checkin
   - 进度接 GET /api/progress
```

交付物：

```text
1. dev 完整演示结果。
2. P0 / P1 问题清单。
3. 如果无 P0，完成目标/任务/打卡/进度前端接 API 的第一步。
4. 如果有 P0，给出 P0 修复结果或明日第一步。
```

## 赵今日任务

主责：

```text
资料管理 -> AI 整理 -> 闪卡 / 测试题 -> 成长问答 的后端迁移
```

任务：

```text
1. 拉取并确认最新 dev。
2. 开始资料模块后端第一步，不等待陈跑通演示。
3. 新建或补充 Material schema。
4. 新建 materials 路由。
5. 新建 materials 表或 store 方法。
6. 实现 GET /api/materials。
7. 实现 POST /api/materials。
8. 暂不接真实 AI。
9. summarize 先保留 mock service 思路，能补 POST /api/materials/{id}/summarize 草案更好。
```

交付物：

```text
1. Material 后端对象和接口代码。
2. GET /api/materials、POST /api/materials 可在 /docs 中看到。
3. Material API 的最小冒烟结果。
4. summarize mock service 的下一步边界。
```

## 共同任务

```text
1. 下午同步一次陈跑通 dev 的结果。
2. 对陈记录的问题标记 P0 / P1 / P2 / 待确认。
3. 对赵的 Material API 做一次接口命名和字段确认。
4. 判断明天是继续修 P0 / P1，还是进入前端接 API + 资料后端迁移。
5. 写今日完成、未完成和明日第一步。
```

优先级：

```text
P0：主流程无法演示，今天必须修。
P1：影响体验、数据一致性或合 main 稳定性，合 main 前要修。
P2：优化项，放到 MVP-1A / MVP-1B。
待确认：需要两人或导师确认。
```

## 今日不做

```text
1. 注册登录
2. PDF 上传
3. 真实大模型接入
4. 正式部署
5. 直接合并 main
6. 赵模块一次性全部迁到后端
7. 在存在 P0 时继续做新 API 接入
```

## 验收标准

今天结束时至少确认：

```text
1. 陈已在最新 dev 跑完整演示。
2. python backend/smoke_api.py 通过。
3. 如果无 P0，陈开始或完成目标/任务/打卡/进度前端接 API 的第一步。
4. 如果有 P0，P0 已修复或明确明日第一步。
5. 赵完成 Material API 第一版代码，或至少完成 schema / router / store 主体。
6. 已形成今日问题清单和明日入口。
```

暂不急着合 `main`。只有满足以下条件再考虑：

```text
1. dev 上完整主线可演示。
2. 没有 P0。
3. P1 都有明确处理结论。
4. 前端接 API 后没有破坏原有演示流程。
```

## 明日入口

如果今天演示通过且无 P0：

```text
1. 陈继续前端目标/任务/打卡/进度接 API，优先完成最小闭环。
2. 赵继续 Material API，并开始 POST /api/materials/{id}/summarize mock service。
3. 复测前端接 API 后的完整演示流程。
```

如果今天演示不通过：

```text
1. 明天只修 P0 / P1。
2. 暂缓 main 合并。
3. 每人回自己的模块分支修复，再合回 dev 联调。
```
