# 2026-07-03 工作安排

分支状态：

```text
当前本地分支：feature/v2-qa-frontend-integration
远程 dev：03deb74 feat: persist agent QA records
feature/agent-ask-mock：已合入 dev，并已删除本地分支和远程分支
```

参与成员：陈、赵

参考文档：

1. `docs/planning/第二版项目计划书.md`
2. `docs/planning/每日工作安排规则.md`
3. `docs/status/当前状态.md`
4. `docs/daily/2026-07-02工作安排.md`

## 当前判断

当前项目已经进入第二版 AI 问答联调收口阶段。

今天开始时，Agent 问答后端底座已经合入远程 dev：

```text
1. POST /api/agent/ask 已支持 goalId、materialId、question、limit。
2. 回答结构已包含 answer、basis、suggestion、references、isFromMaterial、confidence。
3. material_qa_records 问答记录持久化已完成。
4. GET /api/materials/{material_id}/qa 已支持按资料读取历史问答。
5. 删除资料时，相关 QA 记录通过外键级联删除。
6. 后端已验证：
   - pytest：15 passed
   - smoke：通过，包含 qa_record_count
   - compileall：通过
7. feature/agent-ask-mock 已完成使命，不再继续使用。
```

当前主线不再是“继续做后端 ask 接口”，而是：

```text
把已合入 dev 的问答后端能力接入前端资料学习流程，并完成浏览器验收。
```

## 今日总目标

完成第二版 AI 问答最小闭环的 dev 联调。

核心闭环：

```text
选择目标
-> 添加资料
-> 生成 chunks
-> 在资料页问 AI
-> POST /api/agent/ask
-> 展示答案、依据、建议、来源和可信度
-> GET /api/materials/{material_id}/qa 读回历史问答
-> 刷新后问答记录仍存在
```

今天不做真实大模型，也不做完整 Agent。今天只保证 mock Agent 问答链路可演示、可验收、可交接。

## 陈今日任务

主责：

```text
dev 后端基线验收 + 问答接口交接 + 状态文档同步
```

### 任务一：确认 dev 后端基线

操作：

```powershell
git status
git log --oneline --decorate -n 5
python -m pytest backend/tests -v
python backend/smoke_api.py
```

验收标准：

```text
1. 当前分支为 dev。
2. origin/dev 最新提交包含 03deb74 feat: persist agent QA records。
3. pytest 通过，预期 15 passed。
4. smoke 通过，并输出 qa_record_count。
```

### 任务二：向赵交接问答接口字段

需要确认并固定：

```http
POST /api/agent/ask
GET  /api/materials/{material_id}/qa
```

请求字段：

```json
{
  "goalId": "goal_xxx",
  "materialId": "material_xxx",
  "question": "这份资料里的核心概念是什么？",
  "limit": 3
}
```

返回字段：

```text
id
materialId
goalId
question
answer
basis
suggestion
sourceTitle
references
isFromMaterial
confidence
createdAt
mode
```

验收标准：

```text
1. 赵能按字段完成前端展示。
2. 资料不足时前端能根据 isFromMaterial=false 和 confidence=low 展示提示。
3. 前端不再猜字段名。
```

### 任务三：更新状态文档

更新：

```text
docs/status/当前状态.md
```

至少同步：

```text
1. 更新时间：2026-07-03。
2. 当前分支：dev。
3. 当前 dev：03deb74 feat: persist agent QA records。
4. 已完成：Agent ask、QA 持久化、GET /qa。
5. 已验证：pytest、smoke、compileall。
6. 未解决问题：前端问 AI 入口浏览器验收、真实大模型 / LangChain、README 配置说明。
7. 最新日文档：docs/daily/2026-07-03工作安排.md。
```

## 赵今日任务

主责：

```text
资料学习链路问答入口 + 问答结果展示
```

### 任务一：接入前端问 AI 入口

前端建议新增或确认：

```js
askAgent({ goalId, materialId, question, limit })
listMaterialQaRecords(materialId)
```

入口建议放在资料总结区或资料详情区。

验收标准：

```text
1. 用户可以在某份资料下输入问题。
2. 点击“问 AI”调用 POST /api/agent/ask。
3. 请求中包含当前 materialId。
4. 如果资料绑定了目标，请求中包含 goalId。
```

### 任务二：展示问答结果

页面至少展示：

```text
1. answer：回答。
2. basis：依据。
3. suggestion：建议。
4. sourceTitle：来源资料。
5. confidence：可信度。
6. isFromMaterial=false 时的资料不足提示。
```

验收标准：

```text
1. 资料中有答案时，页面能展示答案和依据。
2. 资料中没有答案时，页面明确提示资料不足。
3. 页面不会只显示一段不带来源的泛回答。
```

### 任务三：读回历史问答

调用：

```http
GET /api/materials/{material_id}/qa
```

验收标准：

```text
1. 页面刷新后能读回该资料的历史问答。
2. 历史问答按资料隔离，不混到其他资料里。
3. 删除资料后，页面不再展示该资料历史问答。
```

## 共同任务

### 任务一：跑第二版最小问答演示

共同跑一遍：

```text
1. 创建或选择一个目标。
2. 添加一份资料并绑定目标。
3. 生成资料摘要。
4. 生成 chunks。
5. 对资料提问一个能命中的问题。
6. 查看 answer、basis、suggestion、sourceTitle。
7. 对资料提问一个无关问题。
8. 查看资料不足提示。
9. 刷新页面。
10. 确认历史问答仍存在。
```

验收标准：

```text
1. 浏览器控制台无明显错误。
2. POST /api/agent/ask 返回 200。
3. GET /api/materials/{material_id}/qa 返回 200。
4. 问答记录保存并可读回。
5. 第一版目标、计划、打卡、进度主流程不被破坏。
```

### 任务二：确认下一阶段是否接真实大模型

只有当今天前后端问答闭环稳定后，才进入下一阶段：

```text
1. 抽象 answer provider。
2. 准备 LangChain / 大模型配置。
3. 补 README 中的大模型配置说明。
4. 准备第二版验收记录。
```

## 今日不做

```text
1. 不做登录注册。
2. 不做 PDF 上传和解析。
3. 不做向量数据库。
4. 不做完整 RAG。
5. 不做真正 Agent 循环。
6. 不做 LangChain memory。
7. 不做自动联网搜索。
8. 不自动把 AI 建议写入任务列表。
9. 不在问答前端入口未验收前接真实大模型。
10. 不直接合并 main。
```

## 验收标准

今天结束时至少达到：

```text
1. 当前开发基线为 dev。
2. feature/agent-ask-mock 已删除。
3. POST /api/agent/ask 可被前端调用。
4. GET /api/materials/{material_id}/qa 可被前端调用。
5. pytest 通过，预期 15 passed。
6. smoke 通过，并包含 qa_record_count。
7. 前端问 AI 入口可演示，或未完成原因明确。
8. 刷新后问答历史可读回，或未完成原因明确。
9. docs/status/当前状态.md 已同步到 2026-07-03。
```

## 明日入口

如果今天完成问答前后端联调：

```text
1. 抽象真实 AI answer provider。
2. 准备 LangChain / 大模型接入方案。
3. 补 README 中的大模型配置说明。
4. 准备第二版验收记录。
```

如果今天前端问答入口未完成：

```text
1. 明天优先继续资料页问 AI 入口。
2. 暂缓真实大模型。
3. 暂缓 dev -> main。
4. 先保证第二版最小问答闭环可演示。
```

## 今日执行记录

已补齐内容：

```text
1. 新建并使用 feature/v2-qa-frontend-integration 作为今日功能分支。
2. 资料总结区新增按资料提问入口。
3. 前端提问时传入 materialId，资料绑定目标时传入 goalId。
4. app/api.js 新增 GET /api/materials/{material_id}/qa 封装。
5. 页面初始化会按资料读回历史问答记录。
6. 提问成功后重新读取该资料历史问答，并展示 answer、basis、suggestion、sourceTitle、confidence。
7. docs/status/当前状态.md 和 docs/modules/资料模块前后端接口对照.md 已同步到 2026-07-03。
```

已完成检查：

```text
node --check app/api.js：通过
node --check app/app.js：通过
node --check app/modules/materials.js：通过
node --check app/modules/chat.js：通过
python -m pytest backend/tests -v --basetemp .pytest_tmp_v2_qa_frontend：15 passed
python backend/smoke_api.py：通过，包含 qa_record_count: 1
python -m compileall backend：通过
```
