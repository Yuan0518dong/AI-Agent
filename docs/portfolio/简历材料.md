# 简历材料

以下 Bullet 只使用截至 2026-07-20 的本地代码、固定数据集和自动化证据；Mock 与受限真实评测的边界应在追问时如实说明。

- 设计并实现 FastAPI 学习 Agent 后端：数据库会话、Origin 校验、限流、持久化 AgentStep、确认恢复、取消互斥与 Decision Guard，覆盖 Tool Registry 和安全回退链路。
- 实现 PDF/Markdown/TXT 内存式摄取与 pgvector 混合检索：原文件不落盘，保留页码/标题定位；PostgreSQL 通过 halfvec HNSW Top 100 候选与原始 vector(2048) cosine 重排后进行 RRF 融合。
- 建立 10 份资料、50 条中文查询的固定离线检索评测，Mock hybrid Recall@5 为 0.94；同时保留 dense 基线、逐条排名、延迟与数据集指纹，未将该结果表述为真实 embedding 结论。
- 建立 20 场景 Mock Agent 回归与 Playwright 端到端测试，覆盖一键试用、创建目标、PDF 上传、引用问答、确认恢复、拒绝、取消及 390px 无障碍/溢出检查；另完成 DeepSeek 20 x 3 真实 Agent 评测并保留逐 Run Token、成本与失败证据（工具选择 0.6333、确认完整性 0.5556，不表述为稳定成功）。
