# AI-Agent 项目进度记录

更新时间：2026-06-30

## 1. 项目概况

```text
项目名称：AI-Agent
项目定位：通用成长学习助手 App
远端仓库：https://github.com/chenwenv/AI-Agent.git
协作成员：陈、赵
当前阶段：静态可运行原型 + 第一版需求、边界、协作文档、6.30日常分工和技术实现方案整理
```

当前原型是纯前端静态页面，可直接打开：

```text
app/index.html
```

## 2. 第一版产品闭环

第一版优先保住这条主线：

```text
目标 -> 资料 -> AI 整理 -> 计划 -> 打卡 -> 进度
```

完整形态可以扩展为：

```text
注册登录 -> 创建成长目标 -> 添加资料 -> AI 整理资料 -> 生成行动计划 -> 完成今日打卡 -> 查看进度
```

阶段口径：

```text
MVP-0：静态 / 本地演示闭环，允许 localStorage 和 mock AI，目标是讲清楚产品流程。
MVP-1：工程化最小版，加入前后端边界、数据库持久化、最小真实 AI 接口或可替换 mock 层。
MVP-2：补注册登录、用户隔离、PDF 上传、部署和更完整测试。
```

## 3. 已完成内容

```text
app/index.html
app/styles.css
app/app.js
README.md
通用成长学习助手App简单版PRD.md
docs/6.30工作分工文档.md
docs/两人协作开发分工文档.md
docs/第一版功能开发边界与分工.md
docs/第一版技术实现方案.md
```

当前静态原型已具备：

```text
1. 创建成长目标
2. 添加学习或成长资料
3. 自动整理资料摘要和知识点
4. 自动生成 3-7 天行动计划
5. 生成闪卡和简单测试题
6. 支持成长问答的本地模拟
7. 支持任务打卡和进度查看
8. 使用 localStorage 保存本地数据
```

## 4. 当前缺口

近期优先补：

```text
1. 按 docs/6.30工作分工文档.md 强化当前 app/ 静态原型。
2. 陈完善目标、计划、今日任务、打卡、进度相关逻辑。
3. 赵完善资料、AI 整理、闪卡、测试题、成长问答相关逻辑。
4. 共同确认数据对象字段命名、API 返回格式和验收标准。
5. 再评审 docs/第一版技术实现方案.md 的 MVP-1A 边界。
6. 确认后初始化 server/ 后端工程。
```

MVP-2 再补：

```text
1. 注册登录
2. 用户数据隔离
3. PDF 上传和解析
4. 正式部署和测试环境
```

## 5. 协作分工

日常开发按 `docs/6.30工作分工文档.md` 推进。

共同负责：

```text
MVP 边界
核心流程
功能优先级
接口字段确认
导师待确认问题
最终演示和验收
```

陈主责：

```text
目标管理 -> 行动计划 -> 今日任务 -> 打卡 -> 进度统计
```

赵主责：

```text
资料管理 -> AI 整理 -> 闪卡 / 测试题 -> 成长问答
```

重大技术边界、数据库表、API 命名、MVP 阶段取舍和最终验收仍然共同评审。

## 6. 分支协作规则

当前仓库使用 4 个分支协作：

```text
main                         稳定版本 / 最终演示版本
dev                          两人功能合并后的测试分支
feature/goal-plan-checkin    陈负责模块分支
feature/material-ai-memory   赵负责模块分支
```

开发流向：

```text
feature/goal-plan-checkin  \
                            -> dev -> main
feature/material-ai-memory /
```

使用规则：

```text
1. 陈的模块先提交到 feature/goal-plan-checkin。
2. 赵的模块先提交到 feature/material-ai-memory。
3. 两个功能分支完成自测后合并到 dev。
4. dev 跑完整主线和联调测试。
5. 测试稳定后再合并到 main。
6. main 只保留稳定可演示版本。
```

## 7. 第一批数据对象

优先考虑：

```text
Goal
Material
MaterialSummary
PlanTask
Flashcard
QuizQuestion
CheckInRecord
```

后续再考虑：

```text
User
AIConversation
WeakPoint
ReviewSchedule
UploadedFile
```

## 8. 第一批建议接口

MVP-1A 优先：

```text
GET  /api/health
GET  /api/goals
POST /api/goals
GET  /api/goals/:id

GET  /api/materials?goalId=:goalId
POST /api/materials
POST /api/materials/:id/summarize

POST /api/goals/:id/plans
GET  /api/goals/:id/tasks
GET  /api/tasks/today
POST /api/tasks/:id/checkin

GET  /api/goals/:id/progress
```

MVP-1B 再补：

```text
PUT    /api/goals/:id
DELETE /api/goals/:id
GET    /api/materials/:id
DELETE /api/materials/:id
POST   /api/materials/:id/flashcards
POST   /api/materials/:id/quiz
POST   /api/ask
POST   /api/tasks/:id/postpone
```

MVP-2 再补认证接口：

```text
POST /api/auth/register
POST /api/auth/login
POST /api/auth/logout
GET  /api/me
```

## 9. 下一步

```text
1. 赵先在 feature/material-ai-memory 分支推进资料、AI 整理、闪卡、测试题和成长问答模块。
2. 陈在 feature/goal-plan-checkin 分支推进目标、计划、今日任务、打卡和进度模块。
3. 两人完成静态原型增强后合并到 dev 测试。
4. dev 测试稳定后再合并到 main。
```
