# 资料模块前后端接口对照

更新时间：2026-07-03

本文记录资料模块 MVP-1 / MVP-2 前端操作与后端 API 的对应关系。当前资料、摘要、闪卡、测试题、学习片段和资料问答记录的核心数据源已从前端本地生成切到 `/api/materials` / `/api/agent` 系列接口；`localStorage` 仅作为页面状态快照和离线兜底。

## 1. 页面初始化

前端入口：

```text
app/app.js -> init() -> loadMaterialDataFromApi()
```

接口调用：

```text
GET /api/materials
GET /api/materials/{material_id}/summary
GET /api/materials/{material_id}/flashcards
GET /api/materials/{material_id}/quiz
GET /api/materials/{material_id}/chunks
GET /api/materials/{material_id}/qa
```

前端处理：

```text
state.materials  <- materials + summary
state.flashcards <- all flashcards
state.quizzes    <- all quiz questions
state.materialChunks <- chunks grouped by materialId
state.materialQaRecords <- qa records grouped by materialId
```

展示位置：

```text
资料库：#material-list
资料总结：#summary-list
闪卡复习：#flashcard
测试题：#quiz-list
学习片段：#summary-list 内的 chunks 展示区
```

## 2. 新增资料并整理

前端入口：

```text
app/app.js -> #material-form submit
```

接口调用顺序：

```text
POST /api/materials
POST /api/materials/{material_id}/summarize
POST /api/materials/{material_id}/flashcards
POST /api/materials/{material_id}/quiz
GET  /api/materials + 派生数据读回
```

请求字段：

```json
{
  "goalId": "",
  "title": "第一章 细胞结构",
  "type": "text",
  "content": "资料正文或 PDF 摘录",
  "url": ""
}
```

网页链接资料：

```json
{
  "goalId": "",
  "title": "课程网页",
  "type": "link",
  "content": "",
  "url": "https://example.com"
}
```

响应字段：

```text
materials: id, goalId, title, type, content, url, createdAt, updatedAt
summary: materialId, overview, keyPoints, difficulties, studyOrder, actionItems, aiMode, createdAt, updatedAt
flashcards: id, materialId, front, back, status, createdAt, updatedAt
quiz: id, materialId, question, type, options, answer, explanation, createdAt, updatedAt
chunks: id, materialId, chunkIndex, content, keywords, createdAt, updatedAt
qaRecords: id, materialId, goalId, question, answer, basis, suggestion, sourceTitle, references, isFromMaterial, confidence, createdAt, mode
```

## 3. 编辑资料

前端入口：

```text
app/modules/materials.js -> startMaterialEdit(id)
app/app.js -> #material-form submit
```

接口调用顺序：

```text
PUT  /api/materials/{material_id}
POST /api/materials/{material_id}/summarize
POST /api/materials/{material_id}/flashcards
POST /api/materials/{material_id}/quiz
GET  /api/materials + 派生数据读回
```

前端处理：

```text
点击“编辑”后回填同一个资料表单；保存时根据 editingMaterialId 选择 PUT。
保存后重新生成摘要、闪卡和测试题，避免派生内容继续使用旧资料。
```

## 4. 删除资料

前端入口：

```text
app/modules/materials.js -> deleteMaterial(id)
```

接口调用：

```text
DELETE /api/materials/{material_id}
GET    /api/materials + 派生数据读回
```

后端行为：

```text
SQLite 外键 ON DELETE CASCADE 删除对应 summary、flashcards、quiz_questions。
```

前端处理：

```text
重新读取资料模块数据；同时清理本地问答会话中的 relatedMaterialIds。
```

## 5. 生成和搜索学习片段

前端入口：

```text
app/modules/materials.js -> renderSummaries()
资料总结区 -> “生成片段”
资料总结区 -> 搜索学习片段
```

接口调用：

```text
POST /api/materials/{material_id}/chunks
GET  /api/materials/{material_id}/chunks
GET  /api/materials/search?query={query}&limit=5
```

前端处理：

```text
1. 页面初始化时按资料读取 chunks，并写入 state.materialChunks。
2. 点击“生成片段”后调用 POST /chunks，并用响应更新当前资料的 chunks。
3. 在资料总结区展示片段序号、关键词和片段内容。
4. 搜索学习片段时调用 /api/materials/search，并在资料总结区上方展示命中片段。
```

用途边界：

```text
当前 chunks 属于第二版 AI 问答前的上下文准备能力。
它是关键词检索和上下文展示，不是完整向量 RAG。
```

## 6. 成长问答和资料问答历史

前端入口：

```text
app/app.js -> #chat-form submit
app/api.js -> agentApi.ask()
app/modules/chat.js -> renderChat()
app/modules/materials.js -> prefillQuestionFromMaterial()
app/modules/materials.js -> askMaterialQuestion()
资料总结区 -> 当前资料下的“问 AI”表单
```

接口调用：

```text
POST /api/agent/ask
GET  /api/materials/{material_id}/qa
```

提问请求：

```json
{
  "question": "How does RAG retrieval answer questions?",
  "goalId": "goal_xxx",
  "materialId": "material_xxx",
  "limit": 3
}
```

响应字段：

```text
answer
basis
suggestion
references
isFromMaterial
confidence
mode
```

前端处理：

```text
1. 用户在成长问答页提交问题。
2. 前端调用 agentApi.ask() 请求 /api/agent/ask。
3. 助手消息展示 answer。
4. 助手消息下方展示 basis、suggestion、confidence、mode 和是否基于资料。
5. references 折叠展示资料标题、chunk 序号、score 和片段内容。
6. isFromMaterial=false 或 confidence=low 时展示资料不足状态。
7. 资料总结区的“问 AI”按钮预填问题、记录 materialId 并切到成长问答页，不自动提交。
8. 资料总结区的独立问答表单可直接围绕当前资料提问。
9. 资料表单提问成功后，立即调用 GET /api/materials/{material_id}/qa 读回历史记录。
10. 页面展示 question、answer、basis、suggestion、sourceTitle、confidence 和资料不足标识。
11. 刷新页面后，初始化流程会重新读取 GET /qa，并按资料隔离展示历史问答。
12. 历史问答可以生成复盘草稿，用户确认后写入正式闪卡。
```

当前边界：

```text
1. 当前正式聊天入口在成长问答页；资料总结区同时提供预填入口和独立资料问答表单。
2. 当前后端返回 mode=mock，尚未接入真实大模型。
3. GET /api/materials/{material_id}/qa 已由后端提供，并已在资料总结区读回展示。
4. 问答 references 来自 chunks 检索结果，前端负责展示来源、资料不足提示和历史记录。
```

## 7. 复盘草稿加入闪卡

前端入口：

```text
app/modules/materials.js -> renderReviewDrafts()
记忆页 -> 复盘草稿 -> “加入闪卡”
记忆页 -> 闪卡复习 -> “已掌握 / 还要复习”
```

接口调用：

```text
POST  /api/materials/{material_id}/flashcards/custom
PATCH /api/materials/{material_id}/flashcards/{flashcard_id}
GET   /api/materials/{material_id}/flashcards
```

创建单张闪卡请求：

```json
{
  "front": "请解释：番茄工作法为什么能提高专注度？",
  "back": "因为它把学习时间拆成专注时间和短休息，帮助减少分心并记录投入时间。"
}
```

更新复习状态请求：

```json
{
  "status": "review"
}
```

前端处理：

```text
1. “转闪卡草稿”使用 QA question / answer 生成 front / back。
2. “记复习点”使用 suggestion 或 basis 生成复习点。
3. 点击“加入闪卡”后调用 POST /flashcards/custom，并把返回卡片加入 state.flashcards。
4. 成功加入后删除对应复盘草稿。
5. 闪卡复习区展示当前第几张、总数和 new / known / review 状态。
6. 点击“已掌握 / 还要复习”后调用 PATCH /flashcards/{flashcard_id} 保存状态。
7. 页面刷新后，初始化流程通过 GET /flashcards 读回正式闪卡。
```

## 8. 当前已接入

```text
1. 页面初始化读取 GET /api/materials。
2. 新增资料先写入 SQLite，再生成摘要、闪卡和测试题。
3. 刷新页面后，资料、摘要、闪卡、测试题仍能从后端读回。
4. 编辑资料走 PUT /api/materials/{material_id}，保存后重新生成派生练习。
5. 删除资料后，资料库和派生练习不再显示该资料的数据。
6. 资料总结区已接入 chunks 生成、读回和关键词搜索展示。
7. 资料总结区已接入按资料提问和历史问答读回展示。
8. 成长问答页已接入 POST /api/agent/ask，并展示 answer、basis、suggestion、references、confidence 和 mode。
9. 资料总结区已提供“问 AI”预填入口，可带入资料问题并切到成长问答页。
10. 复盘草稿已支持确认加入正式闪卡，并保存 known / review 状态。
```

## 9. 暂未接入

```text
1. 资料摘要、闪卡、测试题仍由后端 mock/rule-based AI service 生成，不是真实大模型。
2. 资料编辑时暂未单独编辑 goalId 归属，当前仍沿用页面选中目标或空目标。
3. chunks 搜索仍是关键词检索，不是 embedding / 向量数据库。
4. 真实大模型 / LangChain 回答尚未接入。
```
